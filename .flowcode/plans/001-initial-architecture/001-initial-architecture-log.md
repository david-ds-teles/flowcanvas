---
name: 001-initial-architecture-log
description: Per-plan execution log for the Flowcanvas v0.1 initial-architecture plan — plan creation, phase ends, and plan completion.
status: active
tags: [plan-log, execution-log, flowcanvas, canvas]
links: [.flowcode/plans/001-initial-architecture/001-initial-architecture-plan.md, .flowcode/plans/001-initial-architecture/001-initial-architecture-design.md, .flowcode/plans/plan-instructions.md]
---

# 001-initial-architecture — Flowcanvas Log

- Per-plan execution record for Flowcanvas v0.1; exactly one log at this path.
- Reverse chronological — newest entry on top, directly below this header; updated at every phase end and at plan end.
- Holds `[PLAN CREATED]` (once), `[PHASE]` (each phase end), `[PLAN COMPLETE]` (once).
- Every entry opens with `**Dev:**`, taken from the session's Acting-as-Dev identity.

---

## [PLAN COMPLETE] — 2026-06-27 (re-close after Phase 8)

**Dev:** david-ds-teles
**Delivered:** Phase 8 closed the reopened plan with seven hands-on polish fixes layered on the shipped v0.1: (1) a behavior-preserving refactor — `app/globals.css` split into six `app/styles/*.css` partials, React Flow state + handlers extracted to a `useCanvasHandlers` hook (`components/canvas/use-canvas-handlers.ts`), static inline styles moved to CSS classes; (2) orthogonal **smoothstep** edges (+ matching live connection-line); (3+4) a **3-size reader** (drawer 440px / half 50vw / full 100vw) with a working `⤢` maximize; (5) **bidirectional `links:` write-back** — drawing or deleting a file↔file edge patches the source `.md`'s `links:` frontmatter through a new guarded `POST /api/canvas/links` (8th route), idempotent against `deriveLinkEdges` on reload; (6) nyx-styled minimap + zoom controls; (7) the Next.js dev badge removed.
**Phases:** 8/8 — all complete (Phases 1–7 v0.1 on 2026-06-26 + Phase 8 polish on 2026-06-27)
**Artifacts:** `001-initial-architecture-technical-overview.md`, `001-initial-architecture-changelog.md`, `001-initial-architecture-test-notes.md`, `001-initial-architecture-qa-report.md` — all updated for Phase 8.
**Follow-ups:** carried from v0.1 — autosave/dirty-flush; per-node revision tracking to populate `MergeReport.conflicts`; symlink-aware path guard; reader relative-image `src` rewriting; de-dupe the import `revision` double-bump; `AGENT_CONTRACT` ⇄ contract-doc sync. New from Phase 8 — surface `patchLinks` write-back failures in the toolbar (currently `console.error` only); if `.md` frontmatter byte-stability is required, replace `matter.stringify` with a surgical `links:`-only line splice (js-yaml re-quotes other scalars).
**Gates:** `tsc 0 · lint 0 · build ok · vitest 66/66 · CDP visual-parity 9/9 · curl /api/canvas/links add/remove + body-preserved + guards 400/400/404`. Review: 0 critical/high, all `≥ medium` findings resolved (Phase 8 medium hook-zone fixed; plan-completion surfaced 1 low — stale doc line — fixed).

---

## [PHASE 8] Post-Execution Polish & Cleanup — complete — 2026-06-27

**Dev:** david-ds-teles
**Started:** 2026-06-27
**Completed:** 2026-06-27
**Built:** Seven post-v0.1 fixes: (1) CSS split — `app/globals.css` base/theme/glass/RF/empty/void rules stay; node/edge/controls/reader/comments/toolbar rules extracted to `app/styles/*.css` (6 new files) `@import`-ed after the Tailwind import; static inline `style={{}}` → CSS classes (`.fc-canvas-root`, image/fallback/group nodes, `.fc-toolbar__group--rel`); RF state + callbacks extracted to new `useCanvasHandlers` hook in `components/canvas/use-canvas-handlers.ts`. (2) Smoothstep — `labeled-edge.tsx` `getBezierPath`→`getSmoothStepPath(borderRadius:8)`; shell `connectionLineType=SmoothStep` + `defaultEdgeOptions={type:'labeled'}`. (3+4) Reader size — store `ReaderSize`/`readerSize`(default `'drawer'`)/`setReaderSize`/`maximizeReader`; `markdown-node.tsx` `⤢`→`maximizeReader`; `reader-drawer.tsx` Drawer·Half·Full segmented control + `data-size`; `app/styles/reader.css` widths 440px/50vw/100vw. (5) Bidirectional links — new `app/api/canvas/links/route.ts` (guarded POST, body-preserving `links:` patch via `frontmatter.stringifyFile`); `api.patchLinks`; store `onConnect` branches (file↔file → deterministic `lk:` edge + `patchLinks(add)`; non-file → user edge); `removeEdgeWriteback` removes edge from `doc.edges` + `patchLinks(remove)`; adapter `deletable:false` per node + shell `deleteKeyCode=['Delete','Backspace']` (edges-only). (6) Nyx controls — `app/styles/controls.css` selectors prefixed `.react-flow ` to outrank RF's stylesheet; `<MiniMap>` nyx hex props. (7) Dev badge — `next.config.ts` `devIndicators: false`. Tests: `store.test.ts` reworked `onConnect` (file↔file/non-file/no-op) + new `removeEdgeWriteback` block + `beforeEach` reader reset. 66/66.
**Files:** `next.config.ts`, `components/canvas/edges/labeled-edge.tsx`, `components/canvas/canvas-shell.tsx`, `components/canvas/use-canvas-handlers.ts` (new), `lib/canvas/store.ts`, `lib/canvas/store.test.ts`, `lib/canvas/adapter.ts`, `lib/canvas/frontmatter.ts`, `lib/api.ts`, `app/api/canvas/links/route.ts` (new), `components/canvas/nodes/markdown-node.tsx`, `components/canvas/reader-drawer.tsx`, `components/canvas/nodes/image-node.tsx`, `components/canvas/nodes/fallback-node.tsx`, `components/canvas/nodes/group-node.tsx`, `components/canvas/canvas-toolbar.tsx`, `app/globals.css`, `app/styles/nodes.css` (new), `app/styles/edges.css` (new), `app/styles/controls.css` (new), `app/styles/reader.css` (new), `app/styles/comments.css` (new), `app/styles/toolbar.css` (new), `docs/flowcanvas-agent-contract.md`, `.flowcode/project/project-overview.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-plan.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-qa-report.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-changelog.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-log.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 66/66 · CDP visual-parity 9/9 · curl /api/canvas/links add/remove+guards 400/400/404` — captures `mockups/captures/phase-8/08-{loaded,reader-full,reader-half,reader-drawer}.png`
**Deviations:** (1) `use-canvas-handlers.ts` placed in `components/canvas/` not `lib/canvas/` — hook contains RF React hooks, violating the pure-TS zone; correct placement is `components/` (qa Finding 1, fixed). (2) Edge deletion re-enabled edges-only via adapter `deletable:false` per node (RF v12 has no `nodesDeletable` prop) + shell `deleteKeyCode=['Delete','Backspace']`; `removeEdgeWriteback` also removes the edge from `doc.edges` for durable deletion on persist. (3) CSS `@import` placed before `@theme` — required for CSS validity; `var()` resolves at runtime so token references inside partials are unaffected. (4) Dev badge suppressed via `devIndicators: false`; shadow-DOM element stays mounted with `offsetParent===null` — CSS fallback was unnecessary. Low/info folded: `patchLinks` errors now `console.error` instead of swallowed (qa Finding 2, low, fixed); `matter.stringify` re-emits other frontmatter via js-yaml — body bytes preserved, scalars may re-quote (qa Finding 3, info, accepted); `beforeEach` resets reader state in `store.test.ts` (qa Finding 4, info, fixed).
**Review:** 0 critical/high · 1 medium (hook zone) fixed · 1 low (silent `patchLinks` error) fixed · 2 info (`matter.stringify` quoting accepted; `beforeEach` reset fixed)

---

## [PLAN COMPLETE] — 2026-06-26

**Dev:** david-ds-teles
**Delivered:** Flowcanvas v0.1 — a standalone Next.js 16 canvas that renders flowcode markdown as nodes, derives + draws labeled edges, pins comment threads, and round-trips the whole board to/from an AI agent as one self-contained JSON (idempotent `DesignBrief` ⇄ `AgentResponse` merge), persisted to a `.canvas` file behind seven guarded fs routes.
**Phases:** 7/7 — all `complete`
**Artifacts:** `001-initial-architecture-technical-overview.md`, `001-initial-architecture-changelog.md`, `001-initial-architecture-test-notes.md`, `001-initial-architecture-qa-report.md`
**Follow-ups:** autosave/dirty-flush; per-node revision tracking to populate `MergeReport.conflicts` (v0.1 is last-writer-wins); symlink-aware path guard; reader relative-image `src` rewriting; node-body inline editing; de-dupe the import `revision` double-bump; enforce `AGENT_CONTRACT` ⇄ contract-doc sync. (Post-exec note: the review/audit sub-agents stalled on the background-agent stream watchdog — final review + code audit were run inline per the pipeline fallback.)

---

## [PHASE 7] Agent Round-Trip & Polish — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** The bidirectional human↔agent JSON loop + the plan-complete chrome that closes Flowcanvas v0.1. New pure `lib/canvas/brief.ts` — agent-round-trip types, `buildBrief` (embeds each markdown node's frontmatter+body, edges with `origin`, comments by id, `intent`), and the 8-step idempotent `applyResponse` merge (returns `{next, report}`), plus the `AGENT_CONTRACT` string. Store gained `mode: CanvasMode` (unifying Phase-6's `commentMode` into `select`/`connect`/`comment`), `addNode`/`addFileNode`, and the `buildBrief()`/`applyResponse()` orchestration (write generated files → re-resolve → re-derive → persist) over a shared `hydrateFiles` helper. New server reader pipeline `lib/render-md.ts` (`remark`→`rehype-sanitize`→`@shikijs/rehype`) behind a guarded `GET /api/render`, surfaced by a glass `ReaderDrawer` on markdown-node click. New chrome: `CanvasToolbar` (modes, add-node menu, upload, Import/Export, fit-view, ⌘S save+dirty-dot), `ExportPanel` (DesignBrief export + AgentResponse import with merge report + stale banner), `FilePicker`, `Dropzone`, and a `page.tsx` error boundary. `docs/flowcanvas-agent-contract.md` ships the human-readable contract.
**Files:** `lib/canvas/brief.ts`, `lib/canvas/brief.test.ts`, `lib/canvas/store.ts`, `lib/canvas/store.test.ts`, `lib/render-md.ts`, `app/api/render/route.ts`, `components/canvas/reader-drawer.tsx`, `components/canvas/export-panel.tsx`, `components/canvas/canvas-toolbar.tsx`, `components/canvas/file-picker.tsx`, `components/canvas/dropzone.tsx`, `components/canvas/comment-layer.tsx`, `components/canvas/canvas-shell.tsx`, `docs/flowcanvas-agent-contract.md`, `app/globals.css`, `app/page.tsx`, `.flowcode/project/project-overview.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-plan.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-ui-design.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-changelog.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-qa-report.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 56/56 (9 brief + 8 store + 39 prior) · dev 200` — `/api/render` curl-verified (real `<pre class="shiki github-dark-default">` + per-token color spans; `../` + non-md → 400). Visual-parity **screenshot- + interactive-CDP-verified** (pure-Node CDP driver, **18/18** checks; `mockups/captures/phase-7/07-{loaded,add-menu,reader,export,import}.png`): toolbar + all 9 controls, connect/comment mode classes, add-node menu (5 items) + add-note + dirty-dot + save-clears, reader drawer opens with 404 chars of shiki prose + close, Export brief JSON (4174 chars, embeds frontmatter/body/responseContract), Import → merge report + stale banner + node created + generated `.md` written. Review: **0 critical/high, 1 medium (project-overview staleness) fixed, 2 low (silent upload errors; export re-build false-stale) fixed, 3 info (ReactNode import fixed; contract-sync + double-revision-bump accepted/deferred)**.
**Deviations:** (1) **Agent-round-trip TYPES live in `brief.ts`** (not a separate types module) — the design located them there. (2) **Idempotency hardened beyond the design snippet:** comments dedup by `id` else by `(parentId,author,text)` content signature, and id-less agent edges that duplicate a directed pair already on the board are skipped (design step 5) — so a re-imported response with id-less replies/edges is a true no-op. `MergeReport.conflicts` is `[]`: v0.1 has no per-node revision tracking (last-writer-wins). (3) **`commentMode` → unified `mode: CanvasMode`** primitive (`select`/`connect`/`comment`); the comment layer reads `mode==='comment'`, replacing the Phase-6 boolean (single-primitive, not parallel surfaces). (4) **Reader uses `@shikijs/rehype` (WASM)** — the Phase-1 swap for the design's native `rehype-shiki`; sanitize runs BEFORE shiki so the highlighter's inline-styled spans survive. (5) **Link/markdown/image add via inline glass `<input>` + `FilePicker`** (no `window.prompt`, honoring the Phase-6 operator ruling). (6) **Empty/error state kept in the shell** (default board + fs-error card from Phase 4) with the plan's "page.tsx" item realized as a React **error boundary** for render-time faults. (7) **`revision` double-bumps per import** (pure step-8 +1, then the server POST +1) — accepted, no correctness impact, deferred (info Finding 6). (8) Added `components/canvas/file-picker.tsx` beyond the plan's file table; table updated in the same edit. (9) **Process catch:** the first CDP run showed the chrome unstyled — dev-server Turbopack served a stale `globals.css` chunk predating the Phase-7 rules (the production build had them all); fixed by `rm -rf .next` + dev restart, re-verified 18/18. Lesson recorded in the ui-design Phase-7 visual-parity row: verify against a clean-cache dev or the production build.

---

## [PHASE 6] Comments Layer — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** Pinned comments — node- or canvas-anchored, flat threads with reply + resolve, over `flowcanvas.comments`. New pure `lib/canvas/comments.ts` anchor math (`anchorForPoint`/`anchorToFlowPoint`), immutable store comment actions (`addComment`/`replyComment`/`resolveComment` + `commentMode`/`setCommentMode`), a `CommentLayer` overlay that projects teardrop pins from live measured React Flow geometry (tracks drag/measure/pan/zoom) and places pins on click in comment mode, a `CommentThread` glass popover that doubles as the draft composer, and a minimal comment-mode toggle in the shell (full toolbar stays Phase 7).
**Files:** `lib/canvas/comments.ts`, `lib/canvas/comments.test.ts`, `lib/canvas/store.ts`, `lib/canvas/store.test.ts`, `components/canvas/comment-layer.tsx`, `components/canvas/comment-thread.tsx`, `components/canvas/canvas-shell.tsx`, `app/globals.css`, `.flowcode/project/project-overview.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-plan.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-ui-design.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-qa-report.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 42/42 (9 comments-math + 7 store-comments + prior 26) · dev 200` — visual-parity **screenshot- + interactive-CDP-verified** (headless Chrome, pure-Node CDP driver): toggle→aria-pressed+hint, node-anchored pin badge 1 + thread (2 rows) + resolve (data-resolved, dims), canvas-anchored pin badge 2 projects correctly; pin teardrop `border-radius 999px/2px` + rose `rgb(255,81,106)` CDP-asserted; captures `mockups/captures/phase-6/06-*.png`; no regressions — review: 0 critical/high/medium, 3 low all fixed (resolve no-op dirty guard; project-overview comments.ts row; project-overview commentMode/setCommentMode), 3 info (memo screen-prop accepted→Phase 7; resolve test gap fixed; plan snippet push→immutable fixed).
**Deviations:** (1) Added `lib/canvas/comments.ts` + `comments.test.ts` beyond the plan's file table — extracted anchor math into a pure DOM-free module for testability, mirroring `edges.ts`/`edges.test.ts`; plan file table updated in the same edit. (2) Comment ids use `crypto.randomUUID()` (consistency with `edgeId`, no extra dep) instead of the plan snippet's `uuid` import; plan snippet rewritten to match (qa Finding 6). (3) `addComment` returns the new id so the layer opens the freshly-created thread (plan snippet was `void`). (4) Pin projection uses live measured RF geometry (`useNodes`/`useViewport`) rather than the authored `doc.nodes` box — required because markdown nodes are auto-height (Phase-4 `height:undefined`), so the authored height would mis-place node-anchored pins. (5) Comment-mode entry is a minimal floating toggle button, not the full toolbar (toolbar is Phase 7 scope). (6) Draft-on-mode-off cleared via a render-phase state reset, not a `useEffect` (lint `react-hooks/set-state-in-effect`). (7) **Tailwind source-scoping (environmental, dev-boot gate):** the dev server 500'd with `TurbopackInternalError: …/flowcode/templates leaves the filesystem root` — Tailwind v4 auto-source-detection followed the dev-mode `.flowcode/*` symlinks (which point outside the project root); fixed by `@import "tailwindcss" source(none)` + explicit `@source` app roots in `globals.css`, mirroring the Phase-1 eslint scoping of the same framework dirs. The production `next build` was unaffected. (8) Durable reload-persistence of comments rides on the Phase-7 save trigger (same deferral as Phase-4 `collapsed` / Phase-5 manual edges) — `save()` already POSTs the full doc incl. `flowcanvas.comments`, which marks dirty on every comment op.
**Follow-up (UX fix — operator feedback, post-close):** operator rejected the native `window.prompt` edge-label dialog and the awkward floating comment affordance. Reworked (polish only, no toolbar): (a) edge labels now edited via an **inline in-canvas glass `<input>`** (`EdgeLabelEditor` in `labeled-edge.tsx`, gated on store `editingEdgeId`/`setEditingEdge`; opens on connect + label double-click; Enter/blur commit, Esc cancel) — both `window.prompt` call sites removed from `canvas-shell.tsx`; (b) comment-mode control → **compact 38×38 icon toggle** (cyan when active); (c) comment thread **tethered to its pin** with a connector beak (`placePopover` flip/clamp + `.fc-cpop__beak`). Touched: `lib/canvas/store.ts` (+`editingEdgeId`/`setEditingEdge`, `onConnect` mints empty-label + opens editor), `lib/canvas/store.test.ts`, `components/canvas/edges/labeled-edge.tsx`, `components/canvas/canvas-shell.tsx`, `components/canvas/comment-layer.tsx`, `components/canvas/comment-thread.tsx`, `app/globals.css`, `.flowcode/project/project-overview.md`. Gates re-verified: `tsc 0 · lint 0 · build ok · vitest 43/43 · dev 200`; interactive-CDP verified (compact icon 38×38; `.fc-edge-input` real `<input>`, commits "realizes" on Enter then closes — no prompt; thread tethered, beak present, ~1px pin→popover gap); captures `mockups/captures/phase-6/06b-*.png`. Review recorded in a follow-up `## Check` in the qa-report.
**Follow-up 2 (connection reliability — operator feedback, post-close):** edge connections were intermittent and a node could self-reference. **Root cause** (found via live CDP `elementFromPoint` hit-testing): `<Handle>`s rendered inside the `.fc-node` card → the `right` + `bottom` handles were occluded by the card (only `top`/`left` were grabbable), so connections only initiated from 2 of 4 sides. **Fixes:** (a) handles moved out of the card into a fragment sibling in all five node components (`markdown`/`image`/`link`/`note`/`fallback`) so they paint above the card subtree — CDP re-verified `isHandle=true` on all four sides; (b) `onConnect` + `isValidConnection` reject self-connections (no self-loop can be drawn); (c) `connectionRadius={34}` + handle CSS prefixed `.react-flow ` to win the cascade (12px dot, `z-index:5`, ~20px `::before` grab target, valid-target cyan highlight). Touched: the five `components/canvas/nodes/*.tsx`, `lib/canvas/store.ts` (+self-guard), `lib/canvas/store.test.ts` (+self-reject test), `components/canvas/canvas-shell.tsx` (`isValidConnection`/`connectionRadius`), `app/globals.css`. Gates: `tsc 0 · lint 0 · build ok · vitest 44/44 · dev 200`; CDP-verified all four handle sides hittable + self-connection rejected; capture `mockups/captures/phase-6/06d-handles.png`.

---

## [PHASE 5] Edges: manual + links-derived — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** Manual + links-derived edges. New `lib/canvas/edges.ts` (`deriveLinkEdges` deterministic `lk:` ids from `links:` frontmatter + `reconcileEdges` keep-user/agent / drop-stale-derived / suppress-manual-dupe), a `memo`'d origin-styled `LabeledEdge` (links muted-dashed+🔒, user solid indigo, agent neon cyan), and store wiring: `load` self-heals the links graph each load, `onConnect` mints user edges, `relabelEdge` promotes a derived edge to user on label-edit, `setNodePosition` commits drag positions (resolves the Phase-4 deferred write-back). Shell registers `edgeTypes` and wires onConnect/onNodeDragStop/onEdgeDoubleClick with `deleteKeyCode={null}`.
**Files:** `lib/canvas/edges.ts`, `lib/canvas/edges.test.ts`, `lib/canvas/store.test.ts`, `components/canvas/edges/labeled-edge.tsx`, `lib/canvas/store.ts`, `components/canvas/canvas-shell.tsx`, `app/globals.css`, `.flowcode/project/project-overview.md`, `001-initial-architecture-ui-design.md` (Phase-5 visual-parity row), `001-initial-architecture-plan.md`, `001-initial-architecture-qa-report.md`, `mockups/captures/phase-5/default-board-edges.png`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 26/26 (11 edges + 6 store + 9 adapter) · dev 200` — visual-parity **screenshot- + CDP-verified** (headless Chrome): the default board derives 1 edge `lk:n-welcome->n-schema`, dashed `5px,4px`, stroke `rgb(144,143,160)` (muted `--color-outline`), label `🔒links`; 5 nodes, no fallback errors — review: 0 critical/high, 1 medium fixed (deleteKeyCode revert), 4 low fixed (DOM-in-lib, missing memo, user-edge color vs design, agent-relabel test), 3 info (selection-clear→Phase 7 deferred, relabel-dirty accepted, project-overview synced).
**Deviations:** (1) Position write-back via `onNodeDragStop`→`setNodePosition` rather than the plan's literal "wire onNodesChange" — committing on drop is the correct RF idiom (writing the store per drag-delta fights the controlled-state sync); resolves Phase-4 deferred low. (2) `onConnect` takes `label` as a param + immutable edge append (plan snippet did in-place `push` + `window.prompt` inside the store) — keeps `lib/canvas/*` DOM-free per project-overview conventions (qa Finding 2). (3) `deriveLinkEdges` adds defensive `readLinks` coercion + per-source dedup beyond the plan snippet. (4) Label-edit promotion implemented via `relabelEdge` + `onEdgeDoubleClick`. (5) Added `store.test.ts` (not in the plan file table) to authoritatively cover the manual-edge/promotion/position store actions. (6) qa Finding 1 (medium): RF default Backspace-delete mutated only local RF state and reverted on the next store sync → `deleteKeyCode={null}` opt-out (store-level delete is Phase 7). (7) qa Finding 4 (low): first-draft STROKE map had links/user colors inverted vs design line 619 → corrected (links muted, user indigo) and CDP re-verified. (8) Durable reload-persistence of manual edges rides on the Phase-7 save trigger (same as the Phase-4 `collapsed` flag) — `save()` already POSTs the doc.

---

## [PHASE 4] Content Nodes — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** The real content layer for the canvas — five `memo` node components (`MarkdownNode`: frontmatter mono table + collapsible rendered body; `ImageNode`: inline `<img>` via `assetUrl` + caption; `LinkChipNode`: `↗` URL chip; `NoteNode`: `text` via `CanvasMarkdown`; `FallbackNode`: catch-all for `group`/non-md `file`) over a thin shared `CanvasMarkdown` renderer — plus the shell rewrite that drops the Phase-2 placeholder/seed, registers the real `nodeTypes`, loads the board (from `?path`, else the default `flowcanvas.canvas`), renders `toReactFlow(doc)`, and shows a minimal empty/error overlay; a shipped **default demo board** (`flowcanvas.canvas` + `examples/welcome.md`/`schema.md`/`architecture.svg`) so the canvas shows real content out of the box; `store.toggleCollapsed` + an immutable `store.load()` refactor; and the Phase-4 node-anatomy + empty-state CSS.
**Files:** `components/canvas/canvas-markdown.tsx`, `components/canvas/nodes/markdown-node.tsx`, `components/canvas/nodes/image-node.tsx`, `components/canvas/nodes/link-node.tsx`, `components/canvas/nodes/note-node.tsx`, `components/canvas/nodes/fallback-node.tsx`, `components/canvas/canvas-shell.tsx`, `lib/canvas/store.ts`, `lib/canvas/adapter.ts`, `lib/canvas/adapter.test.ts`, `app/globals.css`, `flowcanvas.canvas`, `examples/welcome.md`, `examples/schema.md`, `examples/architecture.svg`, `.flowcode/project/project-overview.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 8/8 · dev 200 · review: 0 critical/high, 1 medium fixed (FallbackNode), 2 low (1 fixed / 1 deferred→Phase 5), 1 info fixed` — visual-parity: **screenshot-verified** via headless Chrome (default board renders all node kinds; bad `?path` shows the error card; no client console errors) — supersedes the earlier MCP-unavailable deferral; ui-design § Visual Parity Phase-4 row updated with the captures.
**Deviations:** (1) qa Finding 1 (medium): `group` + non-md/non-image `file` kinds were left unregistered after `PlaceholderNode` removal → added `FallbackNode` and registered it for both, `project-overview.md § Node components` synced. (2) qa Finding 3 (low, fixed): removed duplicate `display:flex`/`flexDirection` inline style in `link-node.tsx` (the `.fc-node--link` class already declares them). (3) qa Finding 4 (info, fixed): `markdown-node.tsx` uses the destructured `id` for `bodyFor(id)`. (4) qa Finding 2 (low, deferred→Phase 5): node drags live only in React Flow state until Phase-5 `onNodesChange` writes positions back to the store — a planned phase gap. (5) Collapse `meta.collapsed` is a persistable field, but the save **trigger** (button/autosave) is Phase 7 — `save()` already POSTs the doc, so collapse round-trips once a Phase-7 save fires. (6) **Entry-point gap (user-caught, fixed):** the first close shipped no way to see content — a bare `localhost:3000` was an empty grid because the shell only loaded a board when `?path` was present and no sample existed. Re-opened the phase: added the default-board load + the shipped `flowcanvas.canvas`/`examples/*` demo + a minimal empty/error overlay, and re-verified visually with headless-Chrome screenshots. The prior "data-path-only / PNG deferred" verification was insufficient — headless Chrome is in fact available and is now the visual-parity method. (7) **Content-node design pass (user-caught, fixed):** the first cards didn't match mockup 04 and had three defects — (a) collapse had no visible effect (fixed React-Flow height); (b) inline images only worked for SVG (embedded markdown images resolved against the page URL, 404); (c) frontmatter showed only 3 fields with purple keys and no chips. Fixed by content-sizing markdown nodes in the adapter (`height: undefined` + `--fc-body-max`, so collapse shrinks; CDP-measured 388→171→388 px) with a `nodrag` toggle, routing embedded images through `/api/asset` in `CanvasMarkdown` (PNG verified), and rebuilding the cards to the mockup (all frontmatter fields, muted keys, semantic status chips + tag chips, mono-cyan headings, glowing bullets, code chips, type badge). qa-report Check 18:10 (Findings 6 [high], 7 [high], 8 [medium] — all fixed).

---

## [PHASE 3] Persistence & Resolve API — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** Six guarded fs Route Handlers (canvas GET/POST with revision bump, batch resolve, asset streaming, markdown file write, directory listing, multipart upload) over a lexical `guardPath` root-confinement guard + a `gray-matter` frontmatter parser, plus typed client wrappers (`lib/api.ts`) and the zustand `useCanvasStore` (`load`/`save` + transient `bodies` cache). Backend persistence + resolve layer for the canvas.
**Files:** `lib/fs-guard.ts`, `lib/canvas/frontmatter.ts`, `app/api/canvas/route.ts`, `app/api/canvas/resolve/route.ts`, `app/api/asset/route.ts`, `app/api/file/route.ts`, `app/api/files/route.ts`, `app/api/upload/route.ts`, `lib/api.ts`, `lib/canvas/store.ts`, `.flowcode/project/project-overview.md`, `.flowcode/plans/001-initial-architecture/001-initial-architecture-design.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 8/8 · dev 200 · curl-acceptance 6/6 routes · review: 0 critical/high, 1 medium fixed, 5 low (4 fixed / 1 deferred→Phase 7), 2 info (accepted / deferred→Phase 4)` — visual-parity N/A (Phase 3 is backend-only, no UI surface); qa-runner gates run inline (matching Phase 1/2 precedent).
**Deviations:** (1) `.mdx` accepted alongside `.md` in `/api/file` + `/api/upload` (consistent with `nodeKind`/resolve + design's MARKDOWN_EXT). (2) `mkdir(dirname,{recursive})` before every write (`file`, `upload`, and `canvas` POST — the last added in-close per qa Finding 2) so nested targets don't 500. (3) `/api/canvas/resolve` returns per-item errors inside a 200 rather than a top-level 400 — design contract updated to match (qa Finding 3). (4) `lib/api.ts` + `store.ts` verbatim from plan snippets. (5) Deferred: `store.save()` not echoing server `updatedAt` → Phase 7 (qa Finding 5); zustand in-place node mutation in `load()` → Phase 4 immutable refactor (qa Finding 8). (6) `store.load()` verified transitively (thin glue over `getCanvas`+`resolvePaths`, both curl-confirmed) — no browser harness this phase.

---

## [PHASE 2] Schema, Adapter & Empty Canvas — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** Typed extended-JSONCanvas schema (`FlowcanvasDoc` + all node/edge/comment types) + bidirectional `FlowcanvasDoc ↔ React Flow` adapter (`toReactFlow`/`toJSONCanvas`/`colorVar`) + a live React Flow canvas (pan/zoom/dot-grid/Controls/MiniMap) rendering two adapter-seeded nyx glass nodes.
**Files:** `lib/canvas/jsoncanvas.ts`, `lib/canvas/adapter.ts`, `lib/canvas/adapter.test.ts`, `components/canvas/canvas-shell.tsx`, `.flowcode/project/project-overview.md`
**Gates:** `tsc 0 · lint 0 · build ok · vitest 8/8 · dev 200 · review: 0 critical/high, 1 medium fixed, 2 low fixed, 2 info (1 fixed/1 accepted)`
**Deviations:** (1) Plan adapter snippet's `React.CSSProperties` (no import) → `import type { CSSProperties }` + `as CSSProperties`. (2) Plan's `toJSONCanvas` edge map hardcoded `toEnd:'arrow'` + `…?.origin as never`, silently erasing edge `color`/`fromEnd` on save → rebuilt with `prevEdgeById` to preserve them (qa-report Finding 1, medium, fixed in-phase). (3) Plan's `nodeTypes = {}` would make React Flow v12 render fallback boxes for the seed → registered a temporary `PlaceholderNode` for all kinds (removed in Phase 4). (4) Seed `SessionMeta` dates given real ISO values. (5) Info: `adapter.ts` legitimately imports `MarkerType` from `@xyflow/react` — project-overview pure-lib rule clarified to exempt it. Visual-parity PNG capture deferred (browser-capture MCP unavailable) — foundation live-verified, no regressions.

---

## [PHASE 1] Project Bootstrap & nyx Visual Foundation — complete — 2026-06-26

**Dev:** david-ds-teles
**Started:** 2026-06-26
**Completed:** 2026-06-26
**Built:** Next.js 16.2.9 + React 19.2.4 + Tailwind v4 app serving the nyx glass void on localhost:3000; nyx `@theme` token system (surfaces/text/accents/neon/radii), local Geist + JetBrains Mono fonts, `cn()` util, vitest config, and an `ssr:false` placeholder CanvasShell.
**Files:** `package.json`, `app/globals.css`, `app/layout.tsx`, `app/page.tsx`, `components/canvas/canvas-shell.tsx`, `lib/utils.ts`, `vitest.config.ts`, `eslint.config.mjs`, `next.config.ts`, `postcss.config.mjs`, `tsconfig.json` (+ removed `public/*.svg`)
**Gates:** `tsc 0 · lint 0 · build ok · dev 200 · unit n/a · review clean (0 critical/high/medium; 2 low fixed, 3 info deferred)`
**Deviations:** create-next-app@latest installed **Next 16.2.9 / React 19.2.4** (not 15 — superset; plan synced); `next lint` removed in 16 → lint script is `eslint`; legacy `rehype-shiki@0.1` (native oniguruma, fails to build on Node 26) replaced by `@shikijs/rehype` + `shiki` (WASM) — reader pipeline is Phase 7; `@types/uuid` dropped (uuid@14 self-types); `vitest` resolved to `^4` (API-compatible). Added Phase-1 placeholder `components/canvas/canvas-shell.tsx` (not in the original file table) so `page.tsx`'s dynamic import resolves and the styled shell renders. Scoped `eslint.config.mjs` to ignore flowcode framework dirs (`.claude/`, `.flowcode/`, `references/`) so the lint gate covers app source only. Removed unused Next demo SVGs. Accepted [info]: `layout.tsx` body uses `var(--font-geist-sans)` directly rather than the `--font-sans` token alias (identical resolution). Visual-parity capture deferred to Phase 2 (no Phase-1 canvas surface to compare; browser-capture MCP unavailable).

---

## [PLAN CREATED] — 2026-06-25

**Dev:** david-ds-teles
**Scope:** Ship Flowcanvas v0.1 — an extended-JSONCanvas board (React Flow) over flowcode markdown files, with `links:`-derived + manual edges, inline images, pinned comments, and a bidirectional human↔agent JSON loop, persisted to a `.canvas` file.
**Phases planned:** 7 — Project Bootstrap & Dark Visual Foundation, Schema/Adapter & Empty Canvas, Persistence & Resolve API, Content Nodes, Edges, Comments Layer, Agent Round-Trip & Polish.
**Design ref:** `001-initial-architecture-design.md`

---
