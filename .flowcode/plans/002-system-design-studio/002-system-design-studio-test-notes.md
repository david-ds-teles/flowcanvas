---
name: 002-system-design-studio-test-notes
description: Test notes for 002-system-design-studio — coverage summary, new tests, manual scenarios, and known gaps generated at plan close.
status: complete
tags: [test-notes, testing, coverage]
links: [.flowcode/plans/002-system-design-studio/002-system-design-studio-plan.md, .flowcode/plans/002-system-design-studio/002-system-design-studio-technical-overview.md]
---

# Test Notes — 002-system-design-studio Flowcanvas System Design Studio (v2)

- 129 vitest tests green at plan close (up from 79 at plan 001 close); all new tests are unit-level pure-module coverage. Integration-level concerns (MCP handshake, route e2e, visual parity) are deferred and tracked as known gaps.
- Status complete; dated 2026-06-28; generated by the post-execution pipeline (sonnet).
- Source plan: `002-system-design-studio-plan.md`; derived from `002-system-design-studio-technical-overview.md`.

---

## Test Coverage Summary

| Layer | Framework | Files Added | Tests Added | Notes |
|-------|-----------|-------------|-------------|-------|
| Unit — pure libs | vitest | 5 new test files | +50 cases (129 − 79) | All pure TS: `refs.test.ts`, `review.test.ts`, `templates.test.ts`, extended `edges.test.ts`, extended `brief.test.ts`, extended `store.test.ts`, extended `adapter.test.ts` |
| Unit — route contracts (Phase 7) | vitest | `app/api/routes-contract.test.ts` | +14 cases (143 total) | Imports each App Router handler, invokes with a real `NextRequest`, asserts contract + guard (400 on path escape, 404 on missing). Now in the `vitest run` gate (`vitest.config.ts` `app/**` + `@` alias). |
| Integration — MCP (Phase 7) | node smoke | `scripts/smoke-mcp.mjs` | 9 assertions | `npm run smoke:mcp` — 7-tool round-trip against a running app; `apply_response` non-stale; merged node persisted. Requires the dev server. |
| E2E — render (Phase 7) | headless Chrome (CDP) | `scripts/smoke-render.mjs` | 5 assertions | `npm run smoke:render` — tri-pane renders, non-zero canvas height, both rails, RF nodes, typed-edge labels. Requires the dev server + Chrome. |

---

## New Tests Added

| Test File | What It Tests | Type | Key Scenarios |
|-----------|--------------|------|--------------|
| `lib/canvas/refs.test.ts` | `extractRefs` — reference extraction | unit | Frontmatter `links:` root-relative (incl. anchor), body `[text](./x.md)` relative resolve + `../` traversal, `![](img.png)` image embed, `https://` external URL flag, dedup of identical `(kind, target, anchor)` tuples, pure-in/pure-out (no fs/DOM). 10 cases incl. the plan's worked example. |
| `lib/canvas/review.test.ts` | `diffDocs` — structural diff | unit | Node add / update / remove; edge add / update / remove; reordered-key non-update (canonical compare); comment added / pre-existing / removed-invariant (removed comments do not appear as changed); `files` passthrough from `roundGeneratedFiles`; empty snapshot → all added. 15 cases. |
| `lib/canvas/templates.test.ts` | `instantiateTemplate` — fragment clone | unit | Fresh `n-*` id minting (node count × fresh ids); coord rebase to `(dropX, dropY)`; `parentId` remapped through id map; external `parentId` fallback to original (not in the template's node set); `meta.origin:'user'` + `meta.template:t.id` provenance; edge `fromNode`/`toNode` remapped; edge `rel`/`label` preserved; `kind:'document'` `files` passthrough; `kind:'node'` empty `files`; plan worked example round-trip. 13 cases. |
| `lib/canvas/edges.test.ts` (extended) | `projectLinksForExport` | unit | Single file→file edge; duplicate edges dedup to one entry; non-file source skipped; non-file target skipped; multi-target on same source; empty doc returns `{}`. +6 cases. |
| `lib/canvas/brief.test.ts` (extended) | `buildBrief` + `applyResponse` v2 contract | unit | Group node create via `applyResponse` (label, `meta.shape`, `parentId`); group node update preserves label from existing when agent omits; typed edge carries `meta.rel` + `label` defaulted from `REL_LABELS`; `buildBrief` emits `refs` for file nodes, `rel` for edges (`origin:'links'` → `'references'`), `parentId` + `source` on nodes; the `0.1` seed `()` cases all pass unchanged (all additions optional). +N cases (plan acceptance criteria: vitest 92/92 at Phase 1 close). |
| `lib/canvas/store.test.ts` (extended) | Store v2 actions | unit | `onConnect` mints typed `user` edge (`rel:'related'`) with no fs write-back; `removeEdgeWriteback` removes only from `doc.edges`; `reviewDiff` returns `null` when no `reviewState`; `reviewDiff` returns a `ReviewDiff` when `reviewState` is set; `navigateRef` focus case (node already on board → `focusNodeId` set); `patchLinks` mock removed. +cases (vitest 129/129 at Phase 4 close). |
| `lib/canvas/adapter.test.ts` (extended) | `meta.rel` round-trip | unit | `meta.rel` set on a `CanvasEdge` survives `toReactFlow` → `RFEdge.data.rel` → `toJSONCanvas` back to `meta.rel`. +1 case. |
| `app/api/routes-contract.test.ts` (Phase 7) | HTTP route contracts | unit (in-gate) | `GET /api/templates` lists the 3 shipped templates + `?id` 404; `GET /api/canvas` returns the demo doc + 400 on non-`.canvas`/path-escape + 404 missing; `POST /api/canvas` revision bump + **`bump:false` no-bump** (D3); review snapshot POST/GET/DELETE round-trip + 400 invalid; active-board POST/GET round-trip + 400 invalid; bundle returns a `PK`-magic zip; `/api/file` write→read→delete + 400 non-`.md` + 400 escape. 14 cases. |

---

## Manual Test Scenarios

Scenarios that require human or browser verification:

Most of these were verified at runtime during the Phase 7 triage (app + MCP sidecar + headless-Chrome CDP + a scripted MCP round-trip and submit→reload→review cycle); scenarios 1/4/9 are also now automated by the Phase 7 smokes + route-contract tests.

| # | Scenario | Steps | Expected Result | Tested |
|---|----------|-------|----------------|--------|
| 1 | MCP round-trip end-to-end | (1) Start the Next.js app; (2) open a `.canvas` board; (3) `npm run mcp`; (4) call `get_active_board` then `get_board`; (5) call `apply_response` with a test `AgentResponse` | `MergeReport` returned (non-stale, D3); merged `.canvas` written to disk; round-ready banner surfaces (D4) | [x] Phase 7 + `npm run smoke:mcp` |
| 2 | Change-review accept flow | (1) Trigger a round via Submit in the inspector; (2) wait for `pendingReview`; (3) reload via the round-ready banner; (4) step through `ReviewPanel`; (5) click Accept | Review opens + lists the round's diff; Accept closes it, `pendingReview` cleared, `.review.json` deleted | [x] Phase 7 (review surfaced + diff listed; accept/discard unit-covered) |
| 3 | Change-review discard flow | (1) Same as #2; (5) click Discard | Board restored to snapshot; generated files deleted; review cleared | [~] discard path unit-covered (`store.test.ts`); not run end-to-end |
| 4 | Template instantiate | (1) Open template tray; (2) click a template card's "Add" button; (3) inspect the board | Template nodes/edges appear at a valid position; `meta.template` provenance set; `meta.origin:'user'` | [x] Phase 7 (tray shows 3 cards; instantiate +3 nodes) |
| 5 | Reference navigation — on-board | (1) Select a file node whose body links to another on-board node; (2) click the `↗` chip | Canvas pans/selects the target node | [x] Phase 7 (FocusBridge centered; node now highlighted — D1) |
| 6 | Reference navigation — off-board | (1) Select the demo `order-service` node (links to off-board `runbook.md`); (2) click the `↗` chip | New file node added near the source; a `rel:'references'` edge drawn from source to new node | [x] Phase 7 (13→14 nodes on the demo board) |
| 7 | Bundle export | (1) Open a board with multiple md nodes and at least one image; (2) click toolbar-bundle | Browser downloads a `.zip`; zip contains the `.canvas`, projected-`links:` md files, image assets, and `bundle-manifest.json` | [x] Phase 7 (valid 7-entry zip + manifest; route-contract test) |
| 8 | Rail collapse / expand | (1) Click `toggle-rail-left`; (2) click `toggle-rail-right` | Structure rail and inspector rail each collapse to `width:0`; canvas expands to fill; re-clicking restores | [x] Phase 7 (264/324 → 0 → restore) |
| 9 | Typed-edge rel picker | (1) Click `edge-rel-pill`; (2) select `'calls'` in `edge-rel-picker` | Edge `meta.rel` updated; eyebrow + label update; board dirty | [x] Phase 7 (picker opens, 8 options, eyebrow updated) |
| 10 | `0.1→0.2` migration | (1) Open a `schemaVersion:'0.1'` board with `links:` frontmatter; (2) reload | Board saves as `0.2`; `links:`-derived edges persisted as `origin:'links'`; no edge silently disappears | [x] Phase 7 (default board served as 0.2; unit + route-contract) |
| 11 | Visual parity at 1280px and 1440px | Load the demo board, expand both rails; compare against `05-studio-canvas.html` | Tri-pane layout, typography, colors, spacing match the nyx design language | [~] 1440 render verified (smoke + capture); 1280 pixel-diff still a follow-up |

---

## Known Gaps

Tests that should exist but were deferred:

| What's Missing | Why Deferred | Follow-up |
|---------------|-------------|-----------|
| ~~MCP handshake integration test~~ | **Closed in Phase 7** | `scripts/smoke-mcp.mjs` (`npm run smoke:mcp`) drives all 7 tools + the full round-trip via `@modelcontextprotocol/sdk` against a running app |
| ~~Route e2e for the new routes~~ | **Closed in Phase 7** | `app/api/routes-contract.test.ts` (14 tests, in the `vitest run` gate) invokes every v2 route handler with a real `NextRequest` |
| Visual-parity pixel capture at 1280 | Partially closed — Phase 7 `scripts/smoke-render.mjs` asserts structure + non-zero canvas at 1440; a per-breakpoint pixel-diff is still unbuilt | Extend the render smoke to diff against mockup screenshots at 1280/1440 |
| `submitToAgent` integration (save + POST review + POST active) | Requires a real fs + running routes; the store test mocks all API calls | Add integration coverage once a route e2e harness exists |
| `discardRound` file-deletion integration | Same — `deleteFileApi` calls are mocked in the store test | Cover via route e2e: verify that `DELETE /api/file` removes the md from disk |
| `navigateRef` off-board path (add node + draw edge) | **Verified at runtime in Phase 7** (demo `runbook.md` chip → +1 node + edge) but not yet an automated assertion | Fold an off-board-add assertion into a future render/Playwright e2e |
| React component tests (`review-panel`, `template-tray`, `structure-rail`, `inspector-rail`) | All four are new Phase-6 components; vitest does not run React component tests today | Add vitest + `@testing-library/react` for the new surfaces; cover submit-mode lock, filter state, and accept/discard button enablement |
