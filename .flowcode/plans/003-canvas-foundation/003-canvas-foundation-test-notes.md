---
name: 003-canvas-foundation-test-notes
description: Test notes for 003-canvas-foundation — coverage summary, new tests, manual scenarios, and known gaps generated at plan close.
status: complete
tags: [test-notes, testing, coverage]
links: [.flowcode/plans/003-canvas-foundation/003-canvas-foundation-plan.md, .flowcode/plans/003-canvas-foundation/003-canvas-foundation-technical-overview.md]
---

# Test Notes — 003-canvas-foundation Canvas Foundation & Visual Integrity

- Suite grew from 147 to 154 tests (+7 unit); a 35-assertion Playwright interaction harness covers all four phases' browser-side behaviour; zero regressions across the plan. Known gaps are React-component rendering (browser-harness-covered per plan acceptance) and the deferred rgba literal alignment.
- Status complete; dated 2026-06-29; generated by the post-execution pipeline (sonnet).
- Source plan: `003-canvas-foundation-plan.md`; derived from `003-canvas-foundation-technical-overview.md`.

---

## Test Coverage Summary

| Layer | Framework | Files Added | Coverage | Notes |
|-------|-----------|-------------|----------|-------|
| Unit | Vitest | 2 new test files; 1 file extended | 154/154 (all green) | `lib/canvas/node-name.test.ts` (5 cases), `lib/canvas/comment-count.test.ts` (2 cases), `lib/canvas/adapter.test.ts` (+2 assertions). Pure-function and primitive-selector coverage only — React components not exercised by vitest. |
| Integration | — | 0 | n/a | `npm run smoke:mcp` not in scope — MCP contract unchanged across plan 003. |
| E2E / Browser | Playwright (ephemeral driver @1512px) | 0 permanent files (harness is ephemeral) | 35/35 assertions across 4 phases | Direct interaction harness exercises resize handles, edge action bar, comment badge, inspector list, frontmatter collapsible, and visual integrity (ring/violet). Console clean in every run. Captures under `mockups/captures/phase-{1,2,3,4}/`. |

---

## New Tests Added

| Test File | What It Tests | Type | Key Scenarios |
|-----------|--------------|------|--------------|
| `lib/canvas/node-name.test.ts` | `nodeDisplayName` pure helper (`lib/canvas/node-name.ts:11`) | Unit | group node → label; link node → url; text node → first line capped at 50 chars (newlines not collapsed, strict split); file-md node → frontmatter.name fallback to basename; file-other node → basename. 5 cases total. |
| `lib/canvas/comment-count.test.ts` | `selectNodeCommentCount` store selector (`lib/canvas/store.ts:111`) | Unit | Returns correct count of unresolved root node-anchored comments for a given node id; returns 0 when no document is loaded (no-doc guard). 2 cases total. |
| `lib/canvas/adapter.test.ts` (+2) | `toReactFlow` edge mapping (`lib/canvas/adapter.ts:41-42`) | Unit | Mapped edge carries `selectable: true`; mapped edge carries `deletable: true`. Added alongside pre-existing adapter cases (suite stays at 147 after Wave 1, then 154 after Phase 3). |

---

## Manual Test Scenarios

Scenarios that require human or browser verification (covered by the Playwright harness during plan execution; listed here for regression reference):

| # | Scenario | Steps | Expected Result | Tested |
|---|----------|-------|----------------|--------|
| 1 | Resize — each node kind | Select a markdown, note, image, or link node; drag a corner handle | 8-handle chrome appears on select; dragging resizes the card; `Cmd-S` + reload restores the new size | [x] |
| 2 | Resize — markdown auto-height | Drag the bottom handle of a markdown node | Card grows/shrinks with content (no inner scrollbar); collapse toggle still works | [x] |
| 3 | Resize — comment-mode handle hide | Enter comment mode; select any node | No resize handles visible; pin-drop click not swallowed | [x] |
| 4 | Edge select — action bar appears | Click an edge | Edge is selected; `EdgeActionBar` renders below the label pill with `rel ▾`, `✎ Label`, `✕` | [x] |
| 5 | Edge action bar — rel picker | Click `rel ▾` on a selected edge | `RelPicker` opens with 8-option grid; choosing an option updates the relation; picker closes | [x] |
| 6 | Edge action bar — label | Click `✎ Label` | Edge label inline-edit activates; `Enter` commits via `relabelEdge` | [x] |
| 7 | Edge action bar — delete | Click `✕` | Edge removed; `data-testid="edge-delete"` is the only delete surface | [x] |
| 8 | Edge — keyboard delete | Select an edge; press `Delete` / `Backspace` | Edge removed via RF keyboard path | [x] |
| 9 | Edge — deselect closes picker | Open `RelPicker`; click canvas to deselect | Picker closes; action bar disappears; no stale state | [x] |
| 10 | Comment badge — zero count | Open a node with no comments | Badge not rendered | [x] |
| 11 | Comment badge — non-zero count | Open a node with unresolved root comments | Rose corner pill shows exact count (or "9+" above cap); `aria-label` carries exact count | [x] |
| 12 | Comment badge — click to select | Click the badge | Node is selected; inspector "Comments on this node" section populates | [x] |
| 13 | Thread header — human name | Open a comment thread anchored to a node | Thread header shows the node's human name (e.g. "Order Service"), not a UUID | [x] |
| 14 | Inspector — comments list | Select a node with comments | "Comments on this node" list shows each comment's badge, author, and preview; row click calls `focusNode` | [x] |
| 15 | Visual — no rest ring | Inspect `.fc-edge-input`, `.fc-note__edit`, `.fc-insp__src`, `.fc-agent__ta` at rest | No visible ring or faded box at rest; crisp ring appears on `:focus` only | [x] |
| 16 | Visual — secondary colour | Inspect `--color-secondary` in devtools | Computes `#e4c6ff` (not the old `#ddb7ff`) | [x] |
| 17 | Reader — frontmatter collapsed | Open reader drawer | Frontmatter shows dense status-pill + first tags + "+N" bar; no kv grid visible | [x] |
| 18 | Reader — frontmatter expand | Click `frontmatter ▾` expander | Full kv grid + links appear; `aria-expanded=true` | [x] |
| 19 | Reader — prose violet reduced | Open reader drawer on a doc with `em` and inline links | `em` renders in `--color-text-primary` (not violet); inline links render in `--color-primary` (indigo); `h3` keeps its violet accent | [x] |

---

## Known Gaps

| What's Missing | Why Deferred | Follow-up |
|---------------|-------------|-----------|
| Vitest unit tests for `NodeResizeFrame` and the four node-component React wrappers | React component rendering is out of vitest scope per plan acceptance criteria; covered by the Playwright harness (Wave 1 14/14 assertions) | Add a Storybook / React Testing Library suite in a dedicated test-hardening task if component-level regression testing is required |
| Vitest unit tests for `CommentBadge` React component | Same as above — React component, covered by browser harness (Phase 3 11/11) | Same follow-up as above |
| Vitest unit tests for `FrontmatterView collapsible` prop | React component; Phase 4 acceptance criteria explicitly state "browser-harness-covered per plan acceptance" | Address in a test-hardening pass |
| Persistent E2E suite (Playwright specs in the repo) | The interaction harness used was ephemeral (not committed to the repo); no `e2e/` directory targets plan 003 scenarios | Convert the harness assertions into committed Playwright specs under `e2e/` before plan 004 lands, to prevent regression |
| `rgba(221,183,255,*)` literal alignment test | The 7-unit colour delta is perceptually negligible and accepted per design constraint; no test enforces token consistency | Add a CSS-token consistency lint or snapshot check in a follow-up cleanup |
