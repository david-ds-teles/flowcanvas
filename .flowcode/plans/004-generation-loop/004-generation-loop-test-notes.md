---
name: 004-generation-loop-test-notes
description: Test notes for 004-generation-loop — coverage summary, new tests, manual scenarios, and known gaps generated at plan close.
status: complete
tags: [test-notes, testing, coverage]
links: [.flowcode/plans/004-generation-loop/004-generation-loop-plan.md, .flowcode/plans/004-generation-loop/004-generation-loop-technical-overview.md]
---

# Test Notes — 004-generation-loop Markdown-Core Generation Loop

- 178/178 vitest tests pass; +20 new tests over Plan 003 close (158 pre-plan → 178 post); pure libs fully unit-covered; React/store/MCP surfaces covered by two app smokes (`smoke:mcp`, `smoke:render`) and six CDP sweeps, all with 0 console errors. Coverage gap: `store` (Phase 4/5 actions), `adapter` (004 routing path), `component-node`, `core-spine`, `export-panel`, `dropzone`, and `mcp/flowcanvas-mcp.ts` are smoke/CDP-only — no direct vitest coverage.
- Status complete; dated 2026-06-29; generated by the post-execution pipeline (sonnet).
- Source plan: `004-generation-loop-plan.md`; derived from `004-generation-loop-technical-overview.md`.

---

## Test Coverage Summary

| Layer | Framework | Files Added | Tests | Notes |
|-------|-----------|-------------|-------|-------|
| Unit — pure libs | vitest | 4 new suites | 16 new + 4 brief round-trip block | `spine.test.ts`, `validate.test.ts`, `migrate.test.ts`, `generation-kit.test.ts` + added block in `brief.test.ts` |
| Unit — route contracts | vitest | 0 new (existing `routes-contract.test.ts` updated) | Assertions updated | `schemaVersion:'0.3'` assertions for demo board + `miniDoc`; verifies the `load`→`migrateDoc` upgrade path |
| Integration — MCP round-trip | node smoke (`npm run smoke:mcp`) | 0 new files | PASS | 8 tools asserted; `get_generation_kit` base + doc-attached + `flowcanvas://generation-kit` resource list + read |
| Integration — render | headless Chrome (`npm run smoke:render`) | 0 new files | PASS | Tri-pane renders, non-zero canvas — no regression from Plan 003 |
| E2E — CDP component widgets | pure-Node CDP driver | 0 new files | PASS | 6 widgets · all 6 leaf kinds · glyphs · `§source` chips · boundary group · clean console |
| E2E — CDP kit modal | pure-Node CDP driver | 0 new files | PASS | Modal + 5-section nav + "Copy full kit" · clean console |
| E2E — CDP spine bidirectional | pure-Node CDP driver | 0 new files | PASS | Spine docked · prose `#auth-service` rehype-slug id (three-way slug parity) · 6 outline rows · 4 badges · component→section highlight · section→component canvas pulse · edit toggle · clean console |
| E2E — CDP import | pure-Node CDP driver | 0 new files | PASS | Paste a `0.x` board → validate → migrate → adopt at minted path; kind node → component widget · clean console |
| E2E — CDP full-sweep | pure-Node CDP driver | 0 new files | PASS | All 5 phases' surfaces in one session · 0 console errors |
| E2E — CDP showcase board | pure-Node CDP driver | 0 new files | PASS | `examples/commerce-system.canvas` — 8 widgets · 3 boundary groups · spine bound · typed edges |

---

## New Tests Added

| Test File | What It Tests | Type | Key Scenarios |
|-----------|--------------|------|--------------|
| `lib/canvas/spine.test.ts` | `slugify`, `outlineOf`, `buildSourceIndex`, `citedDocPaths`, `normPath` | Unit | Three-way slug parity — `slugify('Order lifecycle')` === `rehype-slug` rendered `<h2 id>` === agent anchor convention; `outlineOf` skips ATX headings inside fenced code blocks; `buildSourceIndex` builds anchor→nodeIds restricted to `coreDocPath`; `citedDocPaths` returns distinct paths in first-seen order |
| `lib/canvas/validate.test.ts` | `parseFlowcanvasDoc`, `flowcanvasDocSchema` | Unit | Accepts valid `0.1`/`0.2`/`0.3` docs; rejects malformed JSON; rejects unknown `ComponentKind`; rejects `meta.kind:'boundary'` on a non-group node; `.passthrough()` preserves unmodeled fields (no silent strip) |
| `lib/canvas/migrate.test.ts` | `migrateDoc` | Unit | `0.1→0.3` full ladder (link edges baked, `schemaVersion:'0.3'`); `0.2→0.3` no-op bump (edges untouched); `0.3` idempotent (returns `migrated:false`); `migrated` flag correctness |
| `lib/canvas/generation-kit.test.ts` | `kitSections`, `buildKit` | Unit | `kitSections()` returns 4 non-empty sections; `schemaContract` contains all 8 `COMPONENT_KINDS`, the slug rule, and the boundary group-only line; `buildKit(md)` appends the markdown payload as section 5; `buildKit()` omits it |
| `lib/canvas/brief.test.ts` (extended) | `buildBrief` / `applyResponse` round-trip for 004 fields | Unit | `componentKind` on `BriefNode` emitted when `meta.kind` is set; `coreDocPath` on `DesignBrief`; `AgentNode.kind` threading to `meta.kind` via `nodeFromAgent`; `AGENT_CONTRACT === kitSections().schemaContract` (single-source assertion) |
| `app/api/routes-contract.test.ts` (updated) | HTTP route contracts for `schemaVersion:'0.3'` | Unit | Demo board `examples/commerce-platform.canvas` now at `schemaVersion:'0.3'` after migration; `miniDoc` fixture assertions bumped to `'0.3'` |

---

## Manual Test Scenarios

Scenarios requiring browser or live-app verification — all passed via CDP at plan close:

| # | Scenario | Steps | Expected Result | Tested |
|---|----------|-------|----------------|--------|
| 1 | Component widgets render correctly | Open `examples/commerce-system.canvas`; inspect canvas | 8 kind-typed component widgets with correct glyphs, per-kind accent colors, `§source` chips; boundary groups tinted via `--node-accent` | [x] |
| 2 | Agent Generation Kit UI | Click "Generation Kit" toolbar button | Kit tab opens in agent panel; 5-section nav shows system prompt / schema contract / MCP loop / worked example / (your doc if bound); "Copy full kit" copies markdown | [x] |
| 3 | Core spine — bidirectional linking | Open `examples/commerce-system.canvas`; click a `§source` chip on a component | Spine pane scrolls to the matching heading and pulses it; clicking a spine outline section heading pulses the matching component(s) on canvas | [x] |
| 4 | Core spine — edit and dirty flag | Click "Edit" toggle in the spine pane; type a change | Dirty banner appears; "Submit changes" becomes active; while `pendingReview` the submit button is blocked | [x] |
| 5 | Import via JSON paste | Open Import tab; paste a valid `0.2` full board JSON | Board validated → migrated to `0.3` → adopted at a minted `<stem>-<rid>.canvas`; kind nodes render as component widgets; no error | [x] |
| 6 | Import via drag-drop | Drag a `.canvas` file onto the canvas | Dirty-guard confirm → file validated → migrated → adopted; md/image drag-drop to the same canvas still adds nodes (no `.canvas` diversion) | [x] |
| 7 | MCP `get_generation_kit` tool | Run `npm run smoke:mcp` | 8 tools listed; `get_generation_kit` returns base kit; doc-attached call appends the markdown; resource list + read return the base kit text | [x] |

---

## Known Gaps

| What's Missing | Why Deferred | Follow-up |
|---------------|-------------|-----------|
| `lib/canvas/store.ts` vitest coverage for 004 actions (`importDoc`, `importCanvasFile`, `setCoreDoc`, `highlightSpineSection`, `highlightComponents`, `clearLinkHighlight`) | Store actions require a running Next.js API for fetch calls to `/api/canvas` and `/api/file`; mocking the full HTTP surface is high cost; covered by CDP full-sweep | Plan 005 or a dedicated integration-test scaffold with `msw` or a local test server |
| `lib/canvas/adapter.ts` — vitest assertions for `renderType:'component'` routing and boundary tint (`meta.kind` present on non-group nodes) | The 004 adapter routing was verified by CDP kinded-fixture sweep, not vitest assertions on the `renderType` value | Add an adapter test fixture with `meta.kind` nodes and assert `renderType:'component'` and `--node-accent` presence |
| `mcp/flowcanvas-mcp.ts` — unit tests for `get_generation_kit` tool handler | MCP server uses `StdioServerTransport`; vitest cannot spawn stdio; covered by `npm run smoke:mcp` | Dedicated MCP test harness (JSON-RPC over stdio) or an HTTP-mode variant |
| `components/canvas/core-spine.tsx` React component tests | Requires React Flow provider + Zustand store + `/api/render` mock; high setup cost; covered by live CDP spine verify | `@testing-library/react` with `msw` for `/api/render` in a future plan |
| `components/canvas/nodes/component-node.tsx` React component tests | Same provider cost as core-spine; verified by CDP kinded-fixture sweep | `@testing-library/react` assertions on `data-kind`, `data-silhouette`, glyph presence |
| `components/canvas/dropzone.tsx` — `.canvas` extension dispatch unit test | Requires a File API mock and store; covered by CDP import sweep | Vitest with `vi.fn()` store mock and synthetic `DataTransfer` |
