---
name: 006-semantic-edges-test-notes
description: Test notes for 006-semantic-edges — coverage summary, new tests, manual scenarios, and known gaps generated at plan close.
status: complete
tags: [test-notes, testing, coverage]
links: [.flowcode/plans/006-semantic-edges/006-semantic-edges-plan.md, .flowcode/plans/006-semantic-edges/006-semantic-edges-technical-overview.md]
---

# Test Notes — 006-semantic-edges Semantic Edges & Connection Ports

- Pure libs are unit-tested (ports geometry, snapAngle, migrate 0.4→0.5 + normalizePorts, validate, store port + setEdgeType actions, brief edgeType round-trip); the React/RF interaction surface is verified by live headless-Chrome CDP (the unit gate can't see RF layout/gestures), plus the two app smokes.
- Status complete; dated 2026-06-30; generated by the post-execution pipeline.
- Source plan: `006-semantic-edges-plan.md`; derived from `006-semantic-edges-technical-overview.md`.

---

## Test Coverage Summary

| Layer | Framework | Files Added/Changed | Coverage | Notes |
|-------|-----------|---------------------|----------|-------|
| Unit | vitest | `ports.test.ts` (new) + `edge-geometry`/`migrate`/`validate`/`store`/`brief` `.test.ts` (extended), `routes-contract.test.ts` | 239/239 pass | All pure `lib/canvas/*` logic + HTTP route contracts |
| Integration | node MCP smoke | `scripts/smoke-mcp.mjs` | PASS | 8-tool round-trip, `apply_response` carries `edgeType` via `z.any()` |
| E2E | headless-Chrome render smoke + CDP | `scripts/smoke-render.mjs` + ad-hoc CDP drivers | PASS | tri-pane renders; four-spec gesture verification (below) |

---

## New Tests Added

| Test File | What It Tests | Type | Key Scenarios |
|-----------|--------------|------|--------------|
| `lib/canvas/ports.test.ts` (new) | `portPoint`/`sideAndT`/`portAt`/`autoPort` | unit | perimeter points per side + t clamp; nearest side/t; hit-radius reuse vs null; geometric auto-side |
| `lib/canvas/edge-geometry.test.ts` | `snapAngle` | unit | 45° snap, length preserved, zero-length returns p |
| `lib/canvas/migrate.test.ts` | `0.4 → 0.5` ladder + `normalizePorts` | unit | ports seeded + `rel → edgeType`; pinned-side branch; ladder seeds on `0.1→0.5`; idempotency |
| `lib/canvas/validate.test.ts` | `'0.5'` enum + passthrough | unit | accepts `0.1`–`0.5`; `fromPort`/`toPort`/`meta.edgeType`/`meta.ports` survive |
| `lib/canvas/store.test.ts` | port actions + `setEdgeType` | unit | `onConnect` reuse vs create + spread-second-dot; `addPort`/`movePort` clamp; `setEdgeType` clears superseded overrides |
| `lib/canvas/brief.test.ts` | `edgeType` agent round-trip | unit | `buildBrief` echoes `edgeType`; `applyResponse` threads it (create defaults `'reference'`, update preserves) |
| `app/api/routes-contract.test.ts` | demo board GET | integration | now expects `schemaVersion:'0.5'` |

---

## Manual Test Scenarios

Verified live via headless-Chrome CDP (the four operator-locked specs); re-runnable from the running app.

| # | Scenario | Steps | Expected Result | Tested |
|---|----------|-------|----------------|--------|
| 1 | Dot-anchored connections | Open a board · inspect edge endpoints + `.fc-port` dots | 14 always-visible dots; an edge endpoint seats ≤0.4px from a dot center (arrowhead in the dot) | [x] |
| 2 | Reuse-or-create on connect | Drag a connection between two nodes | One new edge; new dot(s) created (8→9 edges, 14→16 dots) | [x] |
| 3 | Drag-to-move a dot | Alt-drag a dot along its side | The dot moves (769→824px); anchored edges follow; no spurious edge | [x] |
| 4 | Flow-typed edges + legend picker | Select an edge · click a legend row (`event`) | Edge restyles to the type (stroke amber→violet rgb(163,113,247)) | [x] |
| 5 | Reusable ColorPicker | Open the edge style panel | `<ColorPicker>` renders with 6 preset chips; also wired into node-format-bar | [x] |
| 6 | Shift-snap line angles | Grab a line, bend it with Shift held | Segment snaps to 45° (45.0°), length preserved; no snap without Shift | [x] |
| 7 | Agent round-trip carries edgeType | `buildBrief` → `applyResponse` (+ MCP `apply_response`) | `edgeType` preserved on create/update; MCP `z.any()` passthrough; smoke:mcp PASS | [x] |

---

## Known Gaps

| What's Missing | Why Deferred | Follow-up |
|---------------|-------------|-----------|
| No vitest for `adapter`/`labeled-edge`/`legend`/`color-picker`/`port-handles` React render | jsdom has no layout/RF; these are covered by CDP + smokes (the project's standing pattern) | Add component tests if a render harness is adopted |
| No automated regression for the Alt-drag / legend-deselect interaction quirks | Gesture-level; CDP-verified once | Fold into a future Playwright e2e suite |
| `meta.rel` retirement not tested (still emitted as legacy) | Intentional one-version retention | Test removal when `rel` is dropped post-0.5 |

> The pure-logic surface is fully unit-tested; the interaction surface is CDP-verified (consistent with plans 001–005).
