---
name: canvas-nodes
description: Module knowledge base for canvas-nodes — the nodeKind-keyed custom node + edge renderer components and the shared CanvasMarkdown utility that form the visual layer of the React Flow canvas.
status: active
tags: [module, public-api, knowledge-base, react-flow, nodes, edges, canvas]
links: [.flowcode/project/project-overview.md, .flowcode/quality-checks/quality-checks-index.md, .flowcode/quality-checks/markdown-quality.md]
---

# Canvas Nodes

- Owns all `nodeKind`-keyed React Flow custom node components plus the `renderType`-routed `ComponentNode`, the `LabeledEdge` custom edge, `CanvasMarkdown` — the lightweight in-node markdown renderer — and (006) `PortHandles` + `NodeResizeFrame` — the shared port-dot and resize wrapper used by every non-group card node; (006 Phase 3) `NodeFormatBar` — the shared floating format bar for text/shape nodes (text + fill colour via shared `<ColorPicker>`).
- Path: `components/canvas/nodes/*` + `components/canvas/edges/{labeled-edge,edge-markers}.tsx` + `components/canvas/canvas-markdown.tsx`; stack: TypeScript 5 / React 19 / `@xyflow/react` ^12.
- Public API: `MarkdownNode`, `ImageNode`, `LinkChipNode`, `NoteNode`, `FallbackNode`, `GroupNode`, `ComponentNode` (node components); `LabeledEdge` (edge component); `EdgeEndMarker` + `edgeMarkerUrl` (per-edge SVG markers, 005-edges); `CanvasMarkdown` (shared renderer); `normalizeUrl` (URL utility); `PortHandles` + `NodeResizeFrame` (006-semantic-edges, shared port-dot/resize frame).
- Every component is `memo()`'d; nodes return a React fragment — card element plus `<Handle>`s as siblings, never nested inside the `overflow:hidden` card; 006 replaces inline SIDES handles with `<PortHandles node>` in `NodeResizeFrame` (and directly in `GroupNode`/`FallbackNode`).
- No dedicated vitest; React coverage is the headless-Chrome smoke (`npm run smoke:render`); `nodeTypes` / `edgeTypes` registration is the sole call site; generated by merged; last updated 2026-06-30.

---

## Purpose

`canvas-nodes` owns every visual component registered in React Flow's `nodeTypes` and `edgeTypes` maps. It provides seven node renderers — `MarkdownNode` for `.md` files (frontmatter card + collapsible body + reader opener), `ImageNode` for image files (lazy inline `<img>` via `/api/asset`), `LinkChipNode` for URL nodes (external link chip with `normalizeUrl` scheme injection), `NoteNode` for freeform `TextNode` content (double-click inline textarea edit), `GroupNode` for spatial framing (SVG shape outline in rectangle/ellipse/diamond + `NodeResizer` + shape-switcher + inline label edit), `FallbackNode` for any unrecognized file kind, and `ComponentNode` for system-design widgets (kind-aware glyph + name + kind eyebrow + one-line role + optional `§source` chip; routed by adapter `renderType = 'component'` for any kinded non-group node) — plus the `LabeledEdge` edge renderer. As of 005-edges, `LabeledEdge` is a full design-tool edge: floating endpoints (anchored to node centers via `useInternalNode` + `edge-geometry`, re-pinnable per side), a routing-selected path (`smoothstep` default · `bezier` · `straight`), per-edge colored SVG markers (via the new `edge-markers.tsx`), a draggable label along the path, draggable line waypoints (grab-to-bend / move / double-click-to-remove), and a `style ▾` panel (routing · line · shared `<ColorPicker>` for colour · end markers · pinned sides · clear bends) alongside the semantic flow-type picker (`EdgeType` × 6 — 006 Phase 3, replaced the `RelationshipType` × 8 `RelPicker`). As of 006 Phase 3, the action bar button is `type ▾` (`EdgeTypePicker`) and the edge style resolves three levels: per-edge override → `EDGE_TYPE_STYLE[edgeType]` → provenance. `CanvasMarkdown` is a shared sub-component used by `MarkdownNode` and `NoteNode` that resolves relative image paths through the asset API. The sole consumer of this module's exports is `canvas-shell.tsx`, which passes the components to React Flow at mount.

### Internal Architecture

```mermaid
graph TD
    CS["canvas-shell (consumer)"]
    MN["MarkdownNode"]
    IN["ImageNode"]
    LCN["LinkChipNode"]
    NN["NoteNode"]
    FN["FallbackNode"]
    GN["GroupNode"]
    CN["ComponentNode"]
    LE["LabeledEdge"]
    EM["EdgeEndMarker (edge-markers)"]
    EG["edge-geometry (pure)"]
    CM["CanvasMarkdown"]
    FV["FrontmatterView (sibling)"]
    ST["useCanvasStore (store)"]
    API["assetUrl (lib/api)"]
    JC["jsoncanvas types"]
    NF["NodeResizeFrame"]
    CB["CommentBadge"]
    CS --> MN
    CS --> IN
    CS --> LCN
    CS --> NN
    CS --> FN
    CS --> GN
    CS --> CN
    CS --> LE
    MN --> CM
    MN --> FV
    MN --> ST
    NN --> CM
    NN --> ST
    IN --> API
    CM --> API
    GN --> ST
    LE --> ST
    LE --> EM
    LE --> EG
    MN --> JC
    IN --> JC
    LCN --> JC
    NN --> JC
    FN --> JC
    GN --> JC
    LE --> JC
    CN --> JC
    CN --> ST
    CN --> FV
    CN --> NF
    CN --> CB
    PH["PortHandles (006)"]
    PT["lib/canvas/ports (006, pure)"]
    NF --> PH
    GN --> PH
    FN --> PH
    LE --> PT
    NFB["NodeFormatBar (006-P3)"]
    CP["ColorPicker (ui/color-picker, 006-P3)"]
    NN --> NFB
    GN --> NFB
    NFB --> CP
    LE --> CP
```

---

## Public API

Concrete signatures only. No prose.

### Functions / Methods

```tsx
// link-node.tsx:10 — prepends https:// to any scheme-less URL; passes through http:, mailto:, tel:, etc.
export function normalizeUrl(url: string): string

// canvas-markdown.tsx:23 — client react-markdown + remark-gfm; resolves relative images via /api/asset
export function CanvasMarkdown({
  children,
  basePath,
}: {
  children: string
  basePath?: string   // source file path used to resolve relative image srcs; default ''
}): JSX.Element

// ── 006-semantic-edges ──────────────────────────────────────────────────────

// port-handles.tsx:25 — renders one always-visible dot per node.meta.ports entry (Handle id = port id,
// positioned at {side,t} via inline offset CSS) PLUS four faint side "add" handles (id = the Side).
// Dot carries data-fc-portid / data-fc-nodeid for the Alt-drag capture listener in use-canvas-handlers.
// commentMode suppression is handled one level up in NodeResizeFrame.
export function PortHandles({ node }: { node: CanvasNode }): JSX.Element

// node-frame.tsx:9-16 — props for the shared resize+port wrapper; all non-group card nodes pass these.
export interface NodeResizeFrameProps {
  id: string
  node: CanvasNode        // 006: was absent; PortHandles reads node.meta?.ports
  selected: boolean
  minWidth: number
  minHeight: number
  children: ReactNode
}

// node-frame.tsx:23 — one shared NodeResizer + PortHandles wrapper. NodeResizer hidden in commentMode.
// PortHandles also hidden in commentMode (node-frame.tsx:39).
// Fragment layout: <NodeResizer> + {children} (the card) + <PortHandles node> — all siblings, never nested.
export function NodeResizeFrame({ id, node, selected, minWidth, minHeight, children }: NodeResizeFrameProps): JSX.Element

// node-format-bar.tsx:43 — floating format bar shown above a selected text or shape node.
// Writes alignment (setNodeAlign), text colour (setNodeColor), fill colour (setNodeFill) through the store.
// 006 Phase 3: text + fill colour controls use the shared <ColorPicker> (components/ui/color-picker.tsx:5,95,100).
// `leading` accepts a ReactNode prepended before the alignment buttons — GroupNode injects its
// rectangle/ellipse/diamond shape-switcher via this prop so everything lives in one bar (group-node.tsx:85).
export function NodeFormatBar({ id, node, leading }: {
  id: string
  node: CanvasNode
  leading?: ReactNode   // optional leading slot — shape-switcher for GroupNode, absent for NoteNode
}): JSX.Element
```

### Classes

Not applicable — all components are functional React components.

### Node Components

Each accepts `NodeProps` from `@xyflow/react`. React Flow injects `id`, `data`, `selected`, and position geometry. `data` is always `{ node: <CanvasNodeVariant> }` in this codebase (populated by the adapter; extracted via cast in each component).

```tsx
// markdown-node.tsx:63
// data: { node: FileNode } — .md / .mdx file
// Reads store: bodyFor(id), toggleCollapsed, maximizeReader
export const MarkdownNode: React.NamedExoticComponent<NodeProps>

// image-node.tsx:34
// data: { node: FileNode } — image extension file (.png/.jpg/.webp/.gif/.avif/.svg)
// Local state: errored: boolean (onError fallback)
export const ImageNode: React.NamedExoticComponent<NodeProps>

// link-node.tsx:38
// data: { node: CanvasLinkNode } — LinkNode (type:'link', url: string)
// Applies normalizeUrl to node.url before rendering the <a>
export const LinkChipNode: React.NamedExoticComponent<NodeProps>

// note-node.tsx:59
// data: { node: TextNode } — TextNode (type:'text', text: string)
// Local state: editing: boolean, draft: string
// Reads store: setNodeText
// Double-click -> inline <textarea>; cmd/ctrl+Enter or blur commits; Esc cancels
export const NoteNode: React.NamedExoticComponent<NodeProps>

// fallback-node.tsx:36
// data: { node: CanvasNode } — catch-all for non-md, non-image file kinds
// Calls nodeKind(node) to derive the kind chip label
export const FallbackNode: React.NamedExoticComponent<NodeProps>

// group-node.tsx:117
// data: { node: CanvasGroupNode }, selected: boolean
// Local state: editing: boolean, draft: string
// Reads store: setNodeLabel, setNodeSize, setNodeShape
// Renders: ShapeOutline SVG (private) + NodeResizer (when selected) + 3-button shape-switcher (when selected)
// Double-click label area -> inline <input>; Enter or blur commits; Esc cancels
export const GroupNode: React.NamedExoticComponent<NodeProps>

// component-node.tsx:98
// data: { node: CanvasNode } — any node with meta.kind set (adapter renderType 'component')
// Reads store:
//   bodyFor(id) — one-line role fallback from markdown body (component-node.tsx:50)
//   highlightSpineSection — subscribed and called by the §source chip (component-node.tsx:51, Phase 4)
// Renders: KindGlyph (inline-SVG per COMPONENT_KIND_META[kind].glyph) + name + kind eyebrow +
//          one-line role + optional §source chip (data-testid="component-source-chip")
//          wrapped in NodeResizeFrame (minWidth:180, minHeight:96) + CommentBadge sibling
// §source chip (Phase 4, component-node.tsx:81-90): rendered as <button className="fc-cmp__src nodrag nopan">.
//   Clicking calls highlightSpineSection(anchor) with e.stopPropagation(). Was a static <span> before Phase 4.
// data-testid="component-node"; data-kind={kind}; data-silhouette={meta.silhouette}
// First consumer of NodeResizeFrame (Phase 2, component-node.tsx:65)
export const ComponentNode: React.NamedExoticComponent<NodeProps>
```

### Edge Component

```tsx
// labeled-edge.tsx:333
// Receives standard EdgeProps plus data (005-edges EdgeData extended by 006, packed by the adapter):
//   { origin?, rel?, edgeType?, routing?, line?, labelT?, color?, fromSide?, toSide?, fromEnd?, toEnd?,
//     points?, fromPortST?, toPortST? }
// origin defaults to 'user'; edgeType defaults to 'reference' (labeled-edge.tsx:339) — 006 Phase 3, drives
//   EDGE_TYPE_STYLE defaults for color/line/ends; routing → 'smoothstep'; line → 'solid' (links ⇒ 'dashed');
//   fromEnd → 'none'; toEnd → 'arrow'; labelT → 0.5.
// 006 Phase 3 style resolution (labeled-edge.tsx:393-397):
//   stroke  = colorVar(d.color) ?? colorVar(ts.color) ?? STROKE[origin]  (per-edge → type → provenance)
//   line    = d.line ?? ts.line ?? (derived ? 'dashed' : 'solid')
//   fromEnd = d.fromEnd ?? ts.fromEnd   (ts = EDGE_TYPE_STYLE[edgeType])
//   toEnd   = d.toEnd  ?? ts.toEnd
// 006 — fromPortST / toPortST (labeled-edge.tsx:52-53): { side: Side; t: number } resolved by the adapter
//   from the edge's fromPort/toPort ConnectionPort ids. When present, the endpoint is pinned to portPoint()
//   instead of the floating-perimeter math. Absent on legacy/port-less edges (005 fallback still active).
// Private sub-components (not exported): EdgeLabelEditor, EdgeTypePicker, EdgeStylePanel, EdgeActionBar
//   (006 Phase 3: RelPicker replaced by EdgeTypePicker — renders EDGE_TYPES grid keyed by
//   EDGE_TYPE_STYLE[t].label + a free-form label input; commits via relabelEdge; calls setEdgeType.)
// Reads store: editingEdgeId, setEditingEdge, relabelEdge, setEdgeType, removeEdgeWriteback,
//   + setEdgeRouting, setEdgeLine, setEdgeColor, setEdgeMarker, setEdgeSide, setEdgeLabelT, setEdgeWaypoints
//   (006 Phase 3: setEdgeRel replaced by setEdgeType — store.ts:79,604)
// RF hooks: useInternalNode (live node rects for floating endpoints), useReactFlow (screenToFlowPosition)
export const LabeledEdge: React.NamedExoticComponent<EdgeProps>
```

```tsx
// edge-markers.tsx:12 — url(#…) ref for one end, or undefined for none/absent (BaseEdge draws no marker)
export function edgeMarkerUrl(edgeId: string, which: 'start' | 'end', end: EdgeEnd | undefined): string | undefined

// edge-markers.tsx:27 — a colored <marker> def for one edge end; renders null for none/absent. Place in <defs>.
export function EdgeEndMarker(props: { edgeId: string; which: 'start' | 'end'; end: EdgeEnd | undefined; color: string }): JSX.Element | null
```

**Default stroke by provenance** (`labeled-edge.tsx:25-30`) — an explicit `data.color` OVERRIDES this (005-edges):

| `EdgeOrigin` | Stroke CSS var |
|---|---|
| `links` | `--color-outline` (dashed by default) |
| `user` | `--color-primary` |
| `agent` | `--color-neon-cyan` |
| `import` | `--color-secondary` |

**Line style → dash** (`labeled-edge.tsx:399-400`): `solid` ⇒ none · `dashed` ⇒ `7 5` · `dotted` ⇒ `1.5 5`.

**`RelationshipType` values** (`jsoncanvas.ts` — legacy; kept on wire but superseded by `EdgeType` in 006 Phase 3):
`references` · `depends-on` · `implements` · `derives-from` · `calls` · `produces` · `informs` · `related`
Migration map `REL_TO_EDGE_TYPE` (`jsoncanvas.ts:134`) converts old values for adapted edges at load time.

**`EdgeType` values** (`jsoncanvas.ts:104-131` — 006 Phase 3, drives `EdgeTypePicker` and `EDGE_TYPE_STYLE`):

| `EdgeType` | Label | Color preset | Line | fromEnd | toEnd |
|---|---|---|---|---|---|
| `data-flow` | data flow | `'5'` (cyan) | solid | none | arrow |
| `request` | request | `'2'` (amber) | solid | none | arrow-open |
| `response` | response | `'2'` (amber) | dotted | none | arrow-open |
| `event` | event | `'6'` (violet) | solid | none | diamond |
| `dependency` | dependency | `#8b93a7` | dashed | none | arrow |
| `reference` | reference | `#6b7280` | dotted | none | circle |

Default `edgeType` is `'reference'` (`labeled-edge.tsx:339`). The type picker (`data-testid="edge-type-picker"`, `labeled-edge.tsx:168`) renders these 6 as buttons labeled via `EDGE_TYPE_STYLE[t].label`; selecting a type applies its legend defaults and clears superseded per-edge overrides (`store.ts:604`). The `Panel` type for the action-bar popovers is `'none' | 'type' | 'style'` (`labeled-edge.tsx:312`). CSS class `.fc-edge-label__rel` on the label pill's type span is a **stable handle** — kept across the rel→type rename to avoid churn (`labeled-edge.tsx:534`). Similarly `.fc-relpick` on the picker wrapper is preserved verbatim (`labeled-edge.tsx:177`).

### HTTP Routes (if applicable)

Not applicable.

### Events / Messages (if applicable)

Not applicable.

### Exceptions / Errors

Not applicable — no custom error types. Image load errors are handled via `onError` → local `errored: boolean` state in `ImageNode` (`image-node.tsx:12-13`).

---

## Usage Examples

```tsx
// canvas-shell.tsx:49-57 — the registration map that wires this module into React Flow.
// This is the only call site; no direct JSX instantiation exists outside canvas-shell. // constructed

import { MarkdownNode } from './nodes/markdown-node'
import { ImageNode }     from './nodes/image-node'
import { LinkChipNode }  from './nodes/link-node'
import { NoteNode }      from './nodes/note-node'
import { GroupNode }     from './nodes/group-node'
import { FallbackNode }  from './nodes/fallback-node'
import { LabeledEdge }   from './edges/labeled-edge'
import { type NodeTypes, type EdgeTypes } from '@xyflow/react'

// Defined at MODULE SCOPE — never inside a component (new object ref on every render
// triggers React Flow to unmount and remount every node).
const nodeTypes: NodeTypes = {
  markdown: MarkdownNode,  // FileNode with .md/.mdx extension
  image:    ImageNode,     // FileNode with image extension
  link:     LinkChipNode,  // LinkNode (node.url)
  note:     NoteNode,      // TextNode (node.text)
  group:    GroupNode,     // GroupNode (type:'group')
  file:     FallbackNode,  // FileNode with non-md, non-image extension
}
const edgeTypes: EdgeTypes = { labeled: LabeledEdge }
const defaultEdgeOptions = { type: 'labeled' }

// React Flow selects the component by matching node.type (the NodeKind key) against nodeTypes.
// The adapter (lib/canvas/adapter.ts) sets node.type = nodeKind(canvasNode) at load time.
// data.node is always the original CanvasNode from the store.
<ReactFlow
  nodeTypes={nodeTypes}
  edgeTypes={edgeTypes}
  defaultEdgeOptions={defaultEdgeOptions}
/>
```

Registration at `components/canvas/canvas-shell.tsx:49-57`. No direct component instantiation call sites exist beyond this map; the headless-Chrome smoke is the integration test.

---

## Database Schema

Not applicable.

---

## Dependencies

**Upstream modules:**

- `lib/canvas/jsoncanvas` — node type definitions (`FileNode`, `LinkNode`, `TextNode`, `GroupNode`, `CanvasNode`), plus `NodeShape`, `RelationshipType`, `RELATIONSHIP_TYPES`, `EdgeOrigin`, `nodeKind()` discriminator; additionally `ComponentKind` type + `COMPONENT_KIND_META` record (per-kind glyph/label/silhouette/accent) consumed by `ComponentNode` (`component-node.tsx:4-5`); 005-edges `LabeledEdge` also imports `EdgeRouting`/`EdgeLineStyle`/`EdgeEnd`/`Side`/`CanvasColor` types + `EDGE_ROUTINGS`/`EDGE_LINE_STYLES`/`EDGE_ENDS` constants (`labeled-edge.tsx:15-18`); `edge-markers.tsx:7` imports the `EdgeEnd` type; `fallback-node.tsx:5-6`
- `lib/canvas/edge-geometry` — pure floating-edge math (`rectIntersectionToPoint`, `sideOf`, `nearestT`, `nearestSegmentIndex`, `snapAngle` [006 — Shift-snap 45° while bending]) consumed by `LabeledEdge` for floating endpoints, label drag, and line-grab bend insertion (`labeled-edge.tsx:20`)
- `lib/canvas/ports` — [006-semantic-edges] pure port geometry: `portPoint(node: Rect, port: ConnectionPort): Point` (absolute perimeter point for a port, consumed by `LabeledEdge` at `labeled-edge.tsx:21` for port-anchored endpoints); `sideAndT` consumed by `use-canvas-handlers.ts` Alt-drag listener (cross-reference `components/canvas/use-canvas-handlers.ts:101-127`); `portAt`, `autoPort` consumed by store `onConnect`
- `lib/canvas/adapter` — `colorVar` (preset/hex → CSS color) consumed by `LabeledEdge` to resolve `data.color` and the swatch chips (`labeled-edge.tsx:19`)
- `components/canvas/edges/edge-markers` — `edgeMarkerUrl` + `EdgeEndMarker` (per-edge colored SVG `<marker>` defs) consumed by `LabeledEdge` (`labeled-edge.tsx:21`)
- `lib/canvas/store` — `useCanvasStore` with actions: `bodyFor`, `toggleCollapsed`, `maximizeReader` (MarkdownNode); `setNodeText` (NoteNode); `setNodeLabel`, `setNodeSize`, `setNodeShape` (GroupNode); `setNodeAlign`, `setNodeColor`, `setNodeFill` (NodeFormatBar — `node-format-bar.tsx:44-46`); `relabelEdge`, `setEdgeType`, `setEditingEdge`, `editingEdgeId`, `removeEdgeWriteback` + 005-edges `setEdgeRouting`/`setEdgeLine`/`setEdgeColor`/`setEdgeMarker`/`setEdgeSide`/`setEdgeLabelT`/`setEdgeWaypoints` (LabeledEdge — 006 Phase 3: `setEdgeRel` replaced by `setEdgeType`, `store.ts:79,604`); `bodyFor`, `highlightSpineSection` (ComponentNode — body for one-line role fallback; highlightSpineSection for §source chip click, Phase 4); `setNodeSize` + `mode` (NodeResizeFrame — `node-frame.tsx:24-25`); `movePort` [006] (use-canvas-handlers, not a node component — cross-reference `components/canvas/use-canvas-handlers.ts:119`); `markdown-node.tsx:5`, `note-node.tsx:9`, `group-node.tsx:5`, `labeled-edge.tsx:14`, `component-node.tsx:6`, `node-frame.tsx:5`
- `components/ui/color-picker` — `ColorPicker` shared reusable colour picker (006 Phase 3); consumed by `EdgeStylePanel` inside `LabeledEdge` for the edge colour control (`labeled-edge.tsx:22,257`) and by `NodeFormatBar` for text + fill colour controls (`node-format-bar.tsx:5,95,100`). Props: `value?: CanvasColor`, `onChange: (c: CanvasColor) => void`, `onClear?: () => void`, `label: string`.
- `lib/api` — `assetUrl(path): string` builds `/api/asset?path=` URL; consumed by `ImageNode` (`image-node.tsx:5`) and `CanvasMarkdown` (`canvas-markdown.tsx:4`)
- `components/canvas/frontmatter-view` — `FrontmatterView` component + `basename` helper; consumed by `MarkdownNode` (`markdown-node.tsx:8`) and `ComponentNode` for display name derivation (`component-node.tsx:8`)
- `components/canvas/nodes/node-frame` — `NodeResizeFrame` shared frame (centralised `NodeResizer` + `PortHandles` + `commentMode` hide, both suppressed in comment mode); consumed by ALL non-group card nodes: `ComponentNode` (`component-node.tsx:9,65`), `MarkdownNode` (`markdown-node.tsx:7,22`), `NoteNode` (`note-node.tsx:9,22`), `ImageNode` (`image-node.tsx:7,40`), `LinkChipNode` (`link-node.tsx:5,22`); 006 added `node: CanvasNode` prop and replaced four fixed handles with `PortHandles`

**External services:**

- `/api/asset` — guarded image-byte route; `ImageNode` and `CanvasMarkdown` resolve all image paths through this route at runtime

**Key libraries:**

- `@xyflow/react` ^12 — `Handle`, `Position`, `NodeResizer`, `NodeProps`, `EdgeProps`, `BaseEdge`, `EdgeLabelRenderer`, `getSmoothStepPath`; 005-edges `LabeledEdge` also uses `getBezierPath`, `getStraightPath`, `useInternalNode` (live node geometry for floating endpoints), and `useReactFlow` (`screenToFlowPosition` for label/waypoint drags)
- `react-markdown` ^10 + `remark-gfm` ^4 — client-side markdown render in `CanvasMarkdown` (`canvas-markdown.tsx:1-2`)
- `react` 19 — `memo`, `useState`, `useRef`, `useEffect`

---

## Configuration & Environment

Not applicable — this module reads no environment variables or config keys directly. The `/api/asset` URL is built by `assetUrl()` in `lib/api.ts`; the underlying `FLOWCANVAS_ROOT` guard is the route handler's concern, not this module's.

---

## Run / Test / Lint

| Action | Command |
|--------|---------|
| Run | `npm run dev` |
| Test (unit) | Not applicable — no vitest for React components |
| Test (integration) | `npm run smoke:render` |
| Lint | `npm run lint` |
| Typecheck | `npx tsc --noEmit` |

Cross-reference full project gates in `.flowcode/quality-checks/quality-gates.md`.

---

## Key Insights

**Conventions & patterns:**

- **Fragment / handle-as-sibling pattern.** Every node returns a `<>...</>` React fragment. The first child is the visible card element; the remaining children are four `<Handle>` elements at `Position.Top/Right/Bottom/Left` (`const SIDES = [Position.Top, Position.Right, Position.Bottom, Position.Left]` in each file). Handles must be siblings of — never nested inside — the card because the card has `overflow:hidden`, which clips the right/bottom handle hit areas. The authoring comment at `markdown-node.tsx:54` documents the invariant. `GroupNode` follows the same rule: `NodeResizer` + shape bar + `fc-group` div + four handles are all flat fragment siblings (`group-node.tsx:59-113`).

- **`memo()` convention.** All six node components are defined as a private `Inner` function and re-exported as `export const X = memo(Inner)`. `LabeledEdge` uses the inline form `memo(function LabeledEdge(...))`. This prevents React Flow from re-rendering a node component when unrelated store slices change. Never remove `memo` from a node or edge export.

- **`nodeTypes` at module scope.** The `nodeTypes` and `edgeTypes` objects in `canvas-shell.tsx` are defined at module scope (outside any component function). Defining them inside a component creates a new object reference on every render; React Flow treats new references as entirely new type registries and unmounts/remounts all nodes. `canvas-shell.tsx:49-57` is the authoritative example.

- **`data.node` cast convention.** `NodeProps.data` is typed as `Record<string, unknown>`. Every node component recovers its specific variant with `(data as { node: XxxNode }).node`. The adapter (`lib/canvas/adapter.ts`) is the only place that populates `data.node`, making the cast safe at runtime.

- **`LabeledEdge` path is routing-selected (005-edges).** `buildPath(routing, …)` picks `getSmoothStepPath` (default, `borderRadius:8`), `getBezierPath`, or `getStraightPath` (`labeled-edge.tsx:89-104`). When the edge has manual waypoints, the renderer builds a polyline/spline through `[source, ...points, target]` instead (`labeled-edge.tsx:381-389`). Stroke color and dash are applied inline on `<BaseEdge>` (not via CSS class) because CSS cannot key off a runtime prop: `colorVar(data.color) ?? STROKE[origin]` resolves the color and `data.line` resolves the dash (`labeled-edge.tsx:398-400`).

- **Floating endpoints read LIVE node geometry (005-edges).** An unpinned end (`data.fromSide`/`toSide` absent) is recomputed each render from the measured node rects pulled via `useInternalNode` (`labeled-edge.tsx:362-378`): `rectIntersectionToPoint` aims the perimeter exit at the next waypoint (or the other node's center) and `sideOf` maps the hit to a React Flow `Position`. A pinned side keeps React Flow's handle-anchored coords. This is why the adapter sets `sourceHandle`/`targetHandle` only for pinned ends.

- **Per-edge colored markers replace shared `context-stroke` markers (005-edges, `edge-markers.tsx`).** Each edge renders its own `<marker>` defs INSIDE its own `<svg>` fragment (the same SVG root as its `<path>`), filled with the edge's resolved stroke color, via `EdgeEndMarker`/`edgeMarkerUrl`. The earlier shared `context-stroke` markers rendered black because `context-fill`/`context-stroke` does not resolve when the marker def lives in a separate `<svg>` root from the referencing path (React Flow's layout). The adapter no longer emits `markerEnd` — the edge component owns markers entirely.

- **Edge action bar is the only delete + style surface.** When an edge is selected, `EdgeActionBar` portals `type ▾ · style ▾ · ✎ Label · ✕` below the label pill (`labeled-edge.tsx:316-331`). `type ▾` (`onType`, `data-testid="edge-action-type"`) opens `EdgeTypePicker` (006 Phase 3: replaced `RelPicker`; renders the 6 `EDGE_TYPES` as buttons keyed by `EDGE_TYPE_STYLE[t].label` plus a free-form label `<input>`; selecting a type calls `setEdgeType` which applies the legend defaults and clears superseded overrides; CSS class `.fc-relpick` is kept as a stable selector handle — 006 QA Finding 1). `style ▾` opens `EdgeStylePanel` (routing · line · shared `<ColorPicker>` for edge colour incl. clear-to-type-default · From/To end markers · From/To pinned sides · clear-bends), each control wired to the matching `setEdge*` store action. `Panel` type is `'none' | 'type' | 'style'` (`labeled-edge.tsx:312`). Popovers reset to closed on deselection via a render-phase guard (no `setState`-in-effect).

- **`CanvasMarkdown` image resolution.** Without `resolveImageSrc`, a relative `![](diagram.png)` in a node body resolves against the Next.js page URL and 404s. `CanvasMarkdown` intercepts the `img` renderer and routes the src through `assetUrl()`. External URLs, `data:`, `blob:`, and already-routed `/api/asset` paths bypass the rewrite (checked by `DIRECT` regex at `canvas-markdown.tsx:8`).

**Gotchas & invariants:**

- **GroupNode pointer-events bypass.** `fc-group` has `pointer-events:none` (CSS). Only SVG shape primitives (`ellipse`/`polygon`/`rect`) have `pointer-events:visiblePainted` (`nodes.css:335-336`), making the hit area match the shape, not the bounding box. React Flow applies inline `pointer-events:all` on the node wrapper, which is forcibly overridden by `:has(.fc-group) { pointer-events:none !important }` (`nodes.css:340`). Resize handles restore pointer events via `:has(.fc-group) .react-flow__resize-control { pointer-events:all }` (`nodes.css:341`).

- **GroupNode selection ring suppression.** React Flow's default `.selected` `box-shadow` glow on the node wrapper is suppressed for group nodes (`nodes.css:343`: `.react-flow__node.selected:has(.fc-group) { box-shadow:none !important }`). The shape SVG stroke manages the selection highlight through `ShapeOutline`'s `selected` prop — solid stroke + `drop-shadow` filter when selected, dashed `7 5` stroke at rest (`group-node.tsx:21-28`).

- **`ShapeOutline` uses `vectorEffect:'non-scaling-stroke'`.** The stroke width stays constant in screen pixels regardless of viewport zoom (`group-node.tsx:26`). Omitting this would make the group outline thicker when zoomed in.

- **`EdgeLabelEditor` double-fire guard.** Pressing Enter triggers `onKeyDown` (calls `finish(true)`) followed immediately by an `onBlur` event. A `closed` ref prevents `finish` from executing twice (`labeled-edge.tsx:139-143`). This guard is load-bearing — removing it causes the label to be written then immediately cleared.

- **[006-semantic-edges] Port-anchored endpoints replace floating-perimeter math when ports exist.** `LabeledEdge` checks `d.fromPortST`/`d.toPortST` first (`labeled-edge.tsx:380-383`): if present, `portPoint(liveRect, {side,t})` from `lib/canvas/ports` gives the exact dot coordinate and the arrowhead seats inside it. The 005 floating-perimeter path (`rectIntersectionToPoint`) is retained as the legacy/port-less fallback for edges that have no port binding. The adapter resolves `fromPort`/`toPort` ConnectionPort ids to `{side,t}` and packs them as `fromPortST`/`toPortST` in edge data; the renderer is read-only relative to the port geometry.

- **[006-semantic-edges] `PortHandles` replaces inline SIDES handles everywhere.** `NodeResizeFrame` renders `<PortHandles node={node} />` as the last sibling in its fragment, suppressed when `commentMode` is active (`node-frame.tsx:39`). `GroupNode` and `FallbackNode` render `<PortHandles node={node} />` directly (they do not use `NodeResizeFrame`) — `group-node.tsx:130`, `fallback-node.tsx:28`. The four faint "add" handles (CSS class `fc-port-add`, id = the Side string) let any node with no ports yet start or receive a connection; `store.onConnect` creates a fresh `ConnectionPort` on that side. Named dot handles (CSS class `fc-port`, id = port id) are the stable anchors that edges reference.

- **[006-semantic-edges] Alt-drag port-move is a CAPTURE-phase window listener in `use-canvas-handlers`, not in the node component.** The listener at `components/canvas/use-canvas-handlers.ts:101-127` intercepts `pointerdown` with `e.altKey` on `.fc-port[data-fc-portid]` elements in the capture phase (before React Flow's handle connection-start fires), calls `sideAndT(r, screenToFlowPosition(...))`, and dispatches `movePort`. Do NOT move this logic into `PortHandles` — capture-phase interception must live above React Flow's event handlers. All edges anchored to a moved dot reposition automatically because `LabeledEdge` reads `portPoint` from live `node.meta.ports` each render.

- **[006-semantic-edges] `snapAngle` in Shift-bend and Shift-waypoint drags.** `edge-geometry.snapAngle(prev, fp)` snaps the dragged point so the segment from `prev` lands on a 45° step. Called in `moveWaypoint` (`labeled-edge.tsx:435`) and in `onLinePointerDown`'s bend-insert drag (`labeled-edge.tsx:469`). Import is at `labeled-edge.tsx:20`.

- **[006-semantic-edges Phase 3] Style resolves three levels deep: per-edge override → `EDGE_TYPE_STYLE[edgeType]` → provenance.** `ts = EDGE_TYPE_STYLE[edgeType]` (`labeled-edge.tsx:340`); then `stroke = colorVar(d.color) ?? colorVar(ts.color) ?? STROKE[origin]`, `line = d.line ?? ts.line ?? (derived ? 'dashed' : 'solid')`, `fromEndShape = d.fromEnd ?? ts.fromEnd`, `toEndShape = d.toEnd ?? ts.toEnd` (`labeled-edge.tsx:393-397`). Calling `setEdgeType` in the store applies the type's legend defaults and clears superseded per-edge overrides (`store.ts:604`). Clearing the colour in `EdgeStylePanel` sets `d.color = undefined`, which reverts to the flow-type colour (`ts.color`), not the provenance colour — the 3-level chain makes this safe.

- **[006-semantic-edges Phase 3] The label pill shows `typeLabel` via `.fc-edge-label__rel` — a stable CSS handle.** `typeLabel = EDGE_TYPE_STYLE[edgeType].label` (`labeled-edge.tsx:341`); rendered as `<span className="fc-edge-label__rel">{typeLabel}</span>` (`labeled-edge.tsx:534`). The class name was NOT renamed from the old `rel` era — existing CSS rules (`.fc-edge-label__rel`, `import`-origin sub-element rule in `studio-shell.css:126`) continue to work without modification. Do not rename this class.

- **[006-semantic-edges Phase 3] `NodeFormatBar` uses the shared `<ColorPicker>` for text + fill colour.** Both the text-colour swatch (`node-format-bar.tsx:95`) and the fill-colour swatch (`node-format-bar.tsx:100`) are now `<ColorPicker>` instances — the same component used by `EdgeStylePanel`. `onClear` is conditionally set only when a value is present, to avoid a spurious clear button when the colour is already unset.

- **`FallbackNode` registered as `'file'`, not `'fallback'`.** The key in `nodeTypes` is `'file'` (`canvas-shell.tsx:55`). The `'group'` key maps to `GroupNode`. The internal comment in `fallback-node.tsx:10` mentions group as a former fallback — that role is now fully replaced by `GroupNode`.

- **`normalizeUrl` is publicly exported but has no external callers.** Only `link-node.tsx:19` calls it internally. The export makes it available for tests or specialized callers, but no other module currently imports it.

- **No dedicated vitest.** React component coverage lives entirely in the headless-Chrome smoke (`npm run smoke:render`, `scripts/smoke-render.mjs`). The 143 vitest tests target only pure `lib/canvas/*` modules. There is no React Testing Library or similar setup in this project.

- **CSS spread across two partials (and one studio partial).** Node styles live in `app/styles/nodes.css`; edge stroke + handle + label editor styles live in `app/styles/edges.css` — 005-edges added the edge Style panel (`.fc-edgestyle*`), the waypoint/add-bend dots, and the handle-noise-reduction rules (connect dots hidden at rest, shown on node-hover/connect) to that partial; the type-picker (`.fc-relpick` — class kept for stability, 006 Phase 3) and the `import`-origin edge label sub-element rule live in `app/styles/studio-shell.css:118-272`. The `NodeFormatBar` (`.fc-fmt-bar`, `.fc-fmt-btn`, `.fc-fmt-group`, `.fc-fmt-div`) also lives in `nodes.css`. The split follows the phase-8 extraction; the studio-shell placement of the picker styles is a minor inconsistency.

---

## Known Gaps

- ~~`NodeResizeFrame` leaf-node migration pending.~~ [RESOLVED 006-semantic-edges] All five leaf nodes (`MarkdownNode`, `ImageNode`, `LinkChipNode`, `NoteNode`, `FallbackNode`) now use `NodeResizeFrame` with a `node` prop; `GroupNode` and `FallbackNode` render `<PortHandles>` directly. No inline `SIDES.map()` blocks remain in any node file.
- The `import` origin label sub-element rule (`.fc-edge-label--import .fc-edge-label__rel`) lives in `app/styles/studio-shell.css:126` rather than `app/styles/edges.css` where the other origin modifier classes sit. A future style consolidation pass should move it.
