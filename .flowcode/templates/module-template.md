---
name: {module-name}
description: Module knowledge base for {Module Name} — public API, schema, dependencies, and key insights an agent needs before changing it.
status: active
tags: [module, public-api, knowledge-base]
links: [.flowcode/project/project-overview.md, .flowcode/quality-checks/quality-checks-index.md, .flowcode/quality-checks/markdown-quality.md]
---

# {Module Name}

- {One-line digest: what this module owns and what depends on it.}
- Path: `{relative/path/to/module}`; stack: {language + framework}.
- Public API: {key entry points — functions, routes, events}.
- Generated at depth by `flowcode:module-explorer-agent` (merge-mode if it exists); meets its § Module Doc Completeness Bar — real signatures, a usage example, config/env, traced deps, conventions.
- Status active; generated by {bootstrap | human | merged}; last updated {DATE}.

---

## Purpose

{One paragraph. What this module owns, what problem it solves, what outside the module depends on it. If the answer is "just glue code", the module may not deserve its own file — roll it into a parent.}

### Internal Architecture

{Non-trivial modules only: a `mermaid` graph of the module's internal components and their dependencies, or its place in the wider system. Omit for simple modules with a one-line reason. Follow `.flowcode/quality-checks/markdown-quality.md § Mermaid`.}

```mermaid
{component / dependency graph}
```

---

## Public API

Concrete signatures only. No prose.

### Functions / Methods

```{language}
# {signature} — {one-line purpose}
# {signature} — {one-line purpose}
```

### Classes

```{language}
class {Name}:
    {field}: {Type}
    def {method}(self, {args}) -> {Return}: ...
```

### HTTP Routes (if applicable)

| Method | Path | Purpose | Request Shape | Response Shape |
|--------|------|---------|---------------|----------------|
| GET | `/x` | | `{Type}` | `{Type}` |

### Events / Messages (if applicable)

| Kind | Topic / Queue | Payload | Direction |
|------|---------------|---------|-----------|
| | | | publish / subscribe |

### Exceptions / Errors

| Name | Raised When | Caught By |
|------|-------------|-----------|
| | | |

---

## Usage Examples

How to call this module's public API. Real only — copied from an actual call site or test, or distilled from a real signature (mark a constructed example `// constructed`).

```{language}
{worked example: import/construct the entry point, call it with concrete args, show the returned shape or side effect}
```

{One line: what the example demonstrates and where the real call site lives (`path:line`).}

---

## Database Schema

Only if this module owns tables. DDL verbatim — no narrative.

```sql
CREATE TABLE ...
```

If the module does not own tables: write "Not applicable" and delete the block.

---

## Dependencies

**Upstream modules:**
- `{module}` — {what this module uses from it}

**External services:**
- `{service}` — {purpose}

**Key libraries:**
- `{lib}` — {purpose}

---

## Configuration & Environment

Module-scoped configuration only — variables and keys THIS module reads. Project-wide env lives in `.flowcode/project/project-overview.md`. If the module reads no configuration: write "Not applicable" and delete the tables.

### Environment Variables

| Variable | Required | Default | Read At (`path:line`) | Purpose |
|----------|----------|---------|-----------------------|---------|
| `{VAR}` | yes/no | `{default}` | `{path}:{line}` | |

### Config Keys

| Key | Source | Type | Purpose |
|-----|--------|------|---------|
| `{key}` | `{config file / store}` | `{type}` | |

---

## Run / Test / Lint

Commands scoped to this module. Cross-reference full project gates in `.flowcode/quality-checks/quality-checks-index.md`.

| Action | Command |
|--------|---------|
| Run | `{command}` |
| Test (unit) | `{command}` |
| Test (integration) | `{command}` |
| Lint | `{command}` |
| Typecheck | `{command}` |

---

## Key Insights

The patterns and conventions this module follows, plus the non-obvious things a future agent must know before changing it.

**Conventions & patterns:** {the module's idioms — layering, naming, error-handling style, the framework features it leans on — traced to real code, not generic advice.}

**Gotchas & invariants:** {subtle invariants, performance notes, historical decisions, known quirks.}

- {insight}
- {insight}

---

## Known Gaps

Items tracked as tech debt or follow-ups specifically inside this module. Cross-reference `.flowcode/project/backlog.md` entries by `BL-NNN` where applicable.

- {gap} — BL-{NNN}
