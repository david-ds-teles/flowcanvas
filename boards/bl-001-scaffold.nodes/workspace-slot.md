---
name: Workspace Slot
description: Main content region — renders the matched App Router page as {children}.
source:
  path: boards/bl-001-scaffold.md
  anchor: app-shell-7-frame
---
The `{children}` prop slot of `AppShell` — the main scrollable content region. In BL-001 each page renders the §12 empty state. Live views land in BL-009→015.
